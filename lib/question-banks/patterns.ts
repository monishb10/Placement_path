import {B,M,S,C,D,type Question} from './types';

export const patternQuestions: Record<string,Question[]> = {
  sorting:B([
    M('Which sequence is sorted in nondecreasing order?', ['[1, 3, 3, 8]','[8, 3, 1]','[1, 8, 3]','[3, 1, 3]'],0,'Nondecreasing order allows equal adjacent values; each value must be at least the previous one.'),
    S('What does it mean for a sorting algorithm to be stable?', 'Records with equal sort keys retain their original relative order. This matters when earlier ordering carries information.'),
    S('Using selection sort in ascending order, what is the array after the first minimum-to-front swap on [7, 2, 5, 1]?', '[1, 2, 5, 7]. The minimum is 1; swap it with the first element, 7.'),
    C('Insertion sort receives [1, 2, 3, 5, 4]. Explain why it can do little work on this input and state its worst-case time.', 'Most elements are already correctly placed. Only 5 shifts when inserting 4. Nearly sorted input can require few shifts, but reverse order still takes O(n²) time.'),
    M('A standard merge sort splits evenly and performs linear merging. Its worst-case time is:', ['O(n)','O(log n)','O(n log n)','O(n²)'],2,'There are O(log n) merge levels and O(n) total merging work per level.'),
    C('Applicants appear as (Ana,80), (Ben,90), (Cara,80). Sort by descending score while retaining arrival order for ties. Give the result and a sorting requirement.', '(Ben,90), (Ana,80), (Cara,80). Use a stable score sort or explicitly include arrival index as a tie-breaker.'),
    S('Why can quicksort with the first element as pivot take quadratic time on an already sorted array?', 'Each partition can leave sizes 0 and n−1, creating n levels with roughly n+(n−1)+… comparisons. Randomized pivots reduce the likelihood, without removing the theoretical worst case.'),
    C('Ten million exam scores are integers from 0 to 100. Propose a sort and compare it with comparison sorting, including space and stability.', 'Count 101 frequencies and reconstruct in O(n+101) time with O(101) counts if only values matter. Stable sorting of attached records needs a position-based counting-sort construction and additional output storage.'),
    D('You must sort a file much larger than RAM. Design the main stages and explain what cost becomes central.', 'Sort chunks that fit in memory, write sorted runs, then merge runs using bounded buffers and a heap of run heads. Discuss disk reads/writes, merge fan-in, temporary storage, and stable tie handling if required.'),
    C('A teammate claims a comparison sort can guarantee O(n) for every array of arbitrary distinct keys. Evaluate the claim and explain why counting sort is not a counterexample.', 'General comparison sorting needs Ω(n log n) comparisons in the worst case: a decision tree must distinguish n! orders. Counting sort uses bounded integer keys and direct indexing, outside the comparison-only model.'),
  ]),
  'binary-search':B([
    M('What property is required for ordinary binary search on an ascending array?', ['Every value must be positive','Values must be sorted','Length must be even','Values must be distinct'],1,'Sorted order makes it safe to discard one side. Duplicates are allowed.'),
    S('What is the worst-case time and auxiliary space of iterative binary search on an array of n elements?', 'O(log n) time and O(1) auxiliary space, since each comparison discards approximately half the remaining range.'),
    S('Search for 7 in [1, 3, 5, 7, 9] using inclusive bounds 0 and 4. After inspecting index 2, what is the new search range?', 'Indices 3 through 4. The middle value 5 is smaller than 7, so set low to mid+1.'),
    C('You need the first occurrence of 2 in [1, 2, 2, 2, 5]. How should a match update the search?', 'Record the matching index and continue to its left by setting high=mid−1. The final answer is index 1; stopping at any match may return a later duplicate.'),
    M('For nonnegative array indices low and high, which midpoint formula avoids overflow from low+high?', ['(low + high) / 2','low + high / 2','low + (high - low) / 2','(high - low) / 2'],2,'The difference stays within the valid index range; adding half the difference to low gives the midpoint without adding both bounds.'),
    S('Define lower bound and find it for target 4 in [1, 3, 5, 5]. What should lower bound return for target 8?', 'Lower bound is the first index whose value is at least the target. It returns 2 for 4 and array length 4 for 8, representing insertion at the end.'),
    C('A loop uses inclusive bounds but sets low=mid when the target is larger. Show a case where it stalls and repair it.', 'For [1,3], low=0, high=1, mid=0, target=3, low remains 0 forever. Use low=mid+1; use high=mid−1 when moving left.'),
    C('Two machines need 2 and 3 seconds per item respectively, working independently from time 0. Find the minimum integer time for 5 items using a monotone predicate.', 'At time t, production is floor(t/2)+floor(t/3). Feasibility is monotone. Time 5 yields 3 items; time 6 yields 5, so answer 6. Binary-search the first feasible time with safe bounds and arithmetic.'),
    D('Design tests for a first-occurrence binary search and explain which implementation mistakes they catch.', 'Include empty, singleton hit/miss, target below/above range, all-equal arrays, and duplicates at both ends. Check −1 for absence and the earliest index for matches; these expose bound errors, premature return, and nonshrinking intervals.'),
    C('You binary-search a minimum feasible capacity, but your feasibility checker sometimes rejects a capacity even though a smaller capacity succeeds. Can binary search still be trusted? Explain how to resolve this.', 'No: the predicate lacks the monotonicity the search assumes. Determine whether the checker is incorrect or the model is nonmonotone. Prove feasibility persists as capacity grows, or use a different search method; spot checks alone do not establish the proof.'),
  ]),
  'two-pointers':B([
    M('For a sorted array, which approach finds whether two distinct elements sum to a target in O(n) time and O(1) extra space?', ['Try every pair','Use pointers at both ends','Generate every subset','Sort each possible pair'],1,'Compare the end values and move one pointer inward; each pointer moves at most n times.'),
    S('In sorted [1, 3, 4, 6], the end values sum to 7 but the target is 9. Which pointer moves, and why?', 'Move the left pointer right. With the current smallest value, moving the right pointer left would only reduce the sum further.'),
    S('What condition prevents using the same element twice in an opposite-end pair-sum scan?', 'Continue only while left < right. Equal indices represent one element, not two distinct elements.'),
    C('Trace an opposite-end search for sum 10 in [1, 2, 4, 6, 8]. Give the first pair found.', '1+8=9, so move left. Then 2+8=10; return values 2 and 8 or indices 1 and 4 according to the contract.'),
    M('A slow pointer writes unique values while a fast pointer scans a sorted array once. The time and extra space are:', ['O(n²), O(1)','O(n), O(n)','O(n), O(1)','O(log n), O(1)'],2,'Both pointers move forward a bounded number of times, using only a few indices.'),
    S('Remove duplicates in place from sorted [1,1,2,2,2,4]. What logical result and length should the method return?', 'The first three positions should be [1,2,4], and the new logical length is 3. Values beyond that prefix are irrelevant.'),
    C('For [0,1,0,3,12], design a linear in-place method to move zeros to the end while preserving nonzero order.', 'Write each nonzero at the next write index, then fill the remaining suffix with zeros. Result [1,3,12,0,0]; O(n) time and O(1) extra space.'),
    C('An unsorted array needs a target-sum pair returned as original indices. Compare sorting with two pointers against a hash-map scan.', 'Sorting value/index pairs preserves original indices but costs O(n log n) time and extra record storage. A map of prior values gives expected O(n) time with O(n) space. Check before inserting to avoid reusing the same element.'),
    D('Justify the pointer move in the container-with-most-water problem when the left height is smaller than the right height.', 'Keeping the shorter left wall and moving the right wall inward decreases width without increasing the limiting height above the left wall. Therefore no such pair beats the current area; discard the shorter wall to seek an improvement.'),
    C('Count distinct value pairs summing to 4 in sorted [1,1,2,2,3,3]. Explain how duplicate handling differs from counting index pairs.', 'Distinct value pairs are (1,3) and (2,2), so 2. After a match, skip duplicate values on both sides. Counting index pairs instead multiplies frequencies for unequal values and uses c(c−1)/2 for equal values; here that count is 5.'),
  ]),
  'sliding-window':B([
    M('A sliding window represents which kind of selection from an array?', ['Any subset','A contiguous segment','Only sorted elements','Only the first and last elements'],1,'The elements between the two window boundaries are consecutive in the original array.'),
    S('For fixed window size k=3, what update replaces recomputing each sum after a one-position move?', 'Subtract the outgoing leftmost element and add the incoming rightmost element. After initializing the first sum, each update is O(1).'),
    S('List the length-2 window sums of [2,1,5,1].', '3, 6, and 6, from [2,1], [1,5], and [5,1].'),
    C('Find the maximum sum of a length-2 subarray in [-4,-2,-7]. Why is initializing the maximum to 0 incorrect?', 'The sums are −6 and −9, so the answer is −6. Initializing to 0 incorrectly permits an empty/nonexistent window; initialize from a real window.'),
    M('A fixed-size sum scan after its initial window typically uses:', ['O(nk) time, O(k) space','O(log n) time, O(1) space','O(n) time, O(1) extra space','O(n²) time, O(n) space'],2,'Maintain a running sum and indices. Each element is added and removed at most once.'),
    S('For nonnegative [2,3,1,2,4,3], find the shortest subarray with sum at least 7 and describe the shrinking rule.', 'Length 2, from [4,3]. Expand right; while sum≥7, record length and remove the left value. Nonnegativity makes shrinking reduce or preserve the sum.'),
    C('Find the longest substring without repeated characters in "abba". Explain how a last-seen-index approach avoids moving the left boundary backward.', 'Maximum length is 2. On a repeat, set left=max(left,lastSeen[c]+1). The final a must not move left behind the current boundary.'),
    C('A shortest-sum-at-least-K algorithm only shrinks while its current sum meets K. Show why it can fail with negative numbers using [1,-1,5], K=5, and suggest an alternative.', 'It records length 3, removes 1, then stops at sum 4, missing [5] of length 1. Prefix sums with a monotone deque can solve the general integer case in O(n) time and O(n) space.'),
    D('Derive a method to count subarrays with exactly K distinct values using an at-most-K counting helper. State a boundary convention.', 'ExactlyK=atMost(K)−atMost(K−1). Maintain counts and shrink until distinct≤limit; each right endpoint contributes right−left+1 valid subarrays. Define atMost(negative)=0. Expected O(n) time with maps and O(n) worst-case storage.'),
    C('A stream needs the maximum of each last k values, with equal values allowed. Design an efficient window structure and explain expiration.', 'Use a deque of indices with nonincreasing values. Expire indices ≤i−k from the front and remove smaller/equal values from the back before inserting i. The front is the maximum; each index enters/leaves once, O(n) time and O(k) space.'),
  ]),
  'prefix-sum':B([
    M('Define P[0]=0 and P[i+1]=P[i]+a[i]. Which expression gives the inclusive sum a[l] through a[r]?', ['P[r]-P[l]','P[r+1]-P[l]','P[l]-P[r+1]','P[r+1]+P[l]'],1,'The prefix through r includes everything before l; subtracting P[l] removes that part.'),
    S('Construct the prefix-sum array with a leading zero for [3,-1,4].', '[0,3,2,6]. Each next entry adds one more original element.'),
    S('After building prefix sums, what are preprocessing and single range-query costs?', 'O(n) preprocessing time and O(n) storage; each valid range-sum query takes O(1) time.'),
    C('For [5,2,-3,7,1], find the sum from zero-based index 1 through 3 using prefix sums.', 'P=[0,5,7,4,11,12]. The sum is P[4]−P[1]=11−5=6.'),
    M('An array has 100,000 entries, each up to 1,000,000. Which Java type should hold its possible total safely?', ['byte','short','int','long'],3,'The total may reach 100,000,000,000, beyond int. Use long prefixes and long arithmetic.'),
    S('Why are ordinary prefix sums inefficient if individual array values are updated between many queries?', 'Changing one element changes every later prefix, so an update may take O(n). A Fenwick tree or segment tree supports updates and queries in O(log n).'),
    C('Find an equilibrium index in [1,3,5,2,2], where sums strictly left and right are equal.', 'Index 2: left=1+3=4 and right=2+2=4. Scan with a running left sum and compute right=total−left−current in O(n) time and O(1) extra space.'),
    C('For [1,-1,1], count subarrays with sum 1 using prefix-frequency counting. Why must prefix 0 have an initial frequency of 1?', 'There are 3. For each current prefix p, add the count of earlier prefixes p−1 before recording p. Initial zero represents the empty prefix, allowing subarrays starting at index 0.'),
    D('Design a static rectangular-sum query structure for a 2D matrix. Explain the inclusion-exclusion query and boundary handling.', 'Use a (rows+1)×(cols+1) prefix table with a zero row/column. A rectangle sum is bottom-right minus the regions above and left, plus their overlap. Preprocessing/storage O(rows×cols); each query O(1).'),
    C('A zero-filled length-6 array receives additions +3 on inclusive [1,4] and −1 on [3,5]. Use a difference array and give the final values.', 'At each l add delta; after r subtract delta using a sentinel at n if needed. The difference array accumulates to [0,3,3,2,2,−1]. For q offline updates, time O(n+q), storage O(n).'),
  ]),
  hashing:B([
    M('What is a hash collision?', ['A key is deleted','Two distinct keys map to the same hash location','A table becomes sorted','A value is always zero'],1,'Different keys can share a hash/bucket location; collision handling must still distinguish the actual keys.'),
    S('Why does computing a hash value not, by itself, prove that two keys are equal?', 'Hash values come from a limited range, so collisions are possible. A correct hash table checks key equality as needed.'),
    S('Count the frequencies of characters in the lowercase string "banana".', 'b:1, a:3, n:2. A small fixed alphabet permits an array of counts; a map also works.'),
    C('Find the first nonrepeating character in "swiss" using a frequency map. Why is a second pass useful?', 'w. First count every character, then scan the original string to select the earliest count-1 character. Map iteration order need not match input order.'),
    M('What is the usual expected lookup time in a well-sized hash table with a suitable hash distribution?', ['O(1)','O(log n) exactly','O(n log n)','O(n²)'],0,'Expected constant time depends on hashing and load assumptions; it is not an unconditional worst-case guarantee.'),
    S('To group lowercase English anagrams, propose a canonical key that distinguishes character counts unambiguously.', 'Use a 26-entry frequency vector serialized with separators or a properly comparable immutable vector key. Raw concatenated counts can be ambiguous, and a plain Java array uses identity equality.'),
    C('Check whether [4,2,4,1] and [1,4,2,4] contain the same multiset. Why is comparing only sets insufficient?', 'They do. Count frequencies and compare counts, or decrement with validation. Sets lose multiplicities: [1,1,2] and [1,2,2] have the same set but different multisets.'),
    C('Find the longest consecutive-value run in unsorted [100,4,200,1,3,2] without sorting. Explain why your scan is expected linear.', 'Put values in a set and extend only from values without a predecessor. Start 1 reaches 4, giving length 4. Each distinct value participates in one run; expected O(n) time/O(n) space. Guard integer endpoints if the full int range is allowed.'),
    D('A rolling hash reports two long substrings have the same fingerprint. Your application requires exact equality. Evaluate the result and propose a verification strategy.', 'Equal fingerprints are candidates, not proof. Compare the actual substrings to verify, or use a deterministic exact algorithm. Multiple hashes lower collision probability but do not provide certainty; include verification cost in complexity.'),
    C('A service accepts attacker-chosen text keys and hash lookup latency becomes erratic. What should be investigated before claiming O(1) performance?', 'Inspect bucket distribution, load, key length, equality/hash cost, resizing, and collision behavior. Consider bounded inputs or collision-resistant table strategies as appropriate. Report measured tail latency and assumptions rather than treating expected time as a guarantee.'),
  ]),
};
