import {B,M,S,C,D,type Question} from './types';

export const placementQuestions: Record<string,Question[]> = {
  dbms:B([
    M('What is the primary purpose of a primary key in a relational table?', ['To sort every result automatically','To uniquely identify each row','To encrypt every column','To allow duplicate row identifiers'],1,'A primary key identifies a row uniquely and cannot contain null key values.'),
    S('What is a foreign key, using Student and Enrollment as an example?', 'Enrollment.student_id can reference Student.student_id, ensuring a referenced student exists for each nonnull enrolled student ID. It models a relationship between tables.'),
    S('Can many Enrollment rows contain the same student_id foreign key? Explain the relationship this models.', 'Yes, unless an additional unique constraint forbids it. One student may have many enrollments, a one-to-many relationship.'),
    C('Each student can enroll in a given course once. Propose keys for Enrollment(student_id,course_id,enrolled_at).', 'Use the pair (student_id,course_id) as a composite primary/unique key, with foreign keys to Student and Course. A separate enrollment ID is also possible if the pair remains uniquely constrained.'),
    M('Which constraint directly prevents two rows from having the same nonnull email address when email is required?', ['A foreign key alone','A UNIQUE constraint plus NOT NULL','An ORDER BY clause','A nonunique index alone'],1,'UNIQUE enforces distinct values and NOT NULL makes email mandatory. Foreign keys check references, not email uniqueness.'),
    S('What is referential integrity, and what happens conceptually when a row references a nonexistent parent under an enforced foreign key?', 'References must point to valid referenced keys, unless null is allowed. An insert/update that violates the enforced constraint is rejected.'),
    C('Deleting a student with existing enrollments could cascade or be restricted. Compare these policies and identify what determines the choice.', 'Cascade removes dependent enrollments automatically; restrict/no-action semantics prevent a violating deletion. Choose based on ownership and retention requirements, and consider soft deletion for records whose history must remain.'),
    C('Two requests both check that username "sam" is unused, then both insert it. Why is an application-level check insufficient, and how do you enforce correctness?', 'The check-then-insert sequence races. Enforce a database unique constraint and handle the losing insertion’s conflict. Use a transaction for related work; a prior lookup may improve messaging but is not the final guarantee.'),
    D('A table stores student_id, student_name, course_id, and course_name for every enrollment. Evaluate duplication and propose a normalized design.', 'Student names repeat per enrollment and course names per student, creating update inconsistencies. Split Student(student_id,name), Course(course_id,name), and Enrollment(student_id,course_id), with keys and references. Discuss joins versus deliberately justified denormalization.'),
    C('Design a transfer of one enrollment from course A to B while preserving “one enrollment per student per course.” What must succeed or fail together?', 'Within a transaction, validate/perform removal from A and insertion into B with relevant foreign and unique constraints. If insertion fails, roll back the removal. Define behavior when A is absent or B already exists; atomicity prevents a partial transfer.'),
  ]),
  os:B([
    M('A process is best described as:', ['A file extension','A running program with execution state and an address space','Only a single CPU instruction','A database table'],1,'A process is an executing program with resources and state managed by the operating system.'),
    S('What is a thread, and how can a process have more than one?', 'A thread is an execution path within a process. Multiple threads can run different tasks while sharing process resources.'),
    S('Name one memory area commonly shared by threads in a process and one normally private to each thread.', 'Threads share the process heap/address space; each thread has its own call stack and execution context.'),
    C('Two threads each execute count++ on shared count=0 without synchronization. Explain how the final result can be 1 rather than 2.', 'Both can read 0, compute 1, then each write 1. Increment is a read-modify-write operation; interleaving loses one update.'),
    M('A context switch involves:', ['Renaming the program file','Saving one execution context and restoring another','Deleting all process memory','Sorting runnable threads by identifier only'],1,'The OS preserves state such as registers and instruction position so another thread/process can run and the original can later resume.'),
    S('Why is concurrency possible on one CPU core even when only one thread executes instructions at a time?', 'The scheduler interleaves execution. Tasks make progress over overlapping periods without simultaneous execution; parallelism requires actual simultaneous execution resources.'),
    C('A user-interface thread performs a long blocking file read and the app stops responding to input. Explain a suitable redesign.', 'Move blocking work to an appropriate background task and deliver the result safely to the UI. Keep UI updates on its required thread and handle cancellation/errors; more threads alone do not guarantee correctness.'),
    C('Thread A holds lock X and waits for Y; thread B holds Y and waits for X. Diagnose the issue and propose a prevention rule.', 'This is deadlock under the stated permanent waits. Require a global lock acquisition order, such as X before Y, to break circular wait. Also consider narrower lock scope and documented ownership.'),
    D('Compare processes and threads for running mutually untrusted extensions in a desktop app.', 'Separate processes provide stronger address-space isolation and failure containment, with IPC and resource overhead. Threads share memory and communicate easily but can corrupt shared state or crash the process. Isolation still needs appropriate OS permissions and boundary validation.'),
    C('A server adds hundreds of threads for CPU-bound work on four cores, and throughput falls. Explain plausible causes and how to investigate.', 'Excess runnable threads add switching, contention, cache disruption, and memory costs without creating more cores. Measure CPU saturation, lock waits, context switches, and latency. Use a bounded worker pool calibrated by workload measurements; distinguish CPU-bound from blocking I/O work.'),
  ]),
  networks:B([
    M('TCP provides an application with:', ['An ordered reliable byte stream','Guaranteed zero latency','Only one-byte datagrams','Automatic end-to-end encryption in all connections'],0,'TCP handles ordered delivery and retransmission within its connection semantics. It does not guarantee latency or provide encryption by itself.'),
    S('What does UDP preserve that TCP’s byte-stream interface does not: message boundaries or application record schemas?', 'UDP preserves datagram/message boundaries. TCP exposes bytes without retaining the boundaries between individual application writes.'),
    S('Does using UDP guarantee that every datagram arrives exactly once and in order?', 'No. Datagrams can be lost, duplicated, or reordered; applications must handle the guarantees they require.'),
    C('A file-transfer application and a live audio application have different needs. Explain why reliable completion matters to one and timely delivery matters to the other.', 'A file needs every byte correct and in order, making a reliable stream useful. Audio may prefer dropping late data rather than delaying playback. Actual protocol design must still account for congestion, security, and the application’s loss tolerance.'),
    M('What extra mechanism is commonly used with TCP to provide transport encryption for HTTPS?', ['TLS','A larger port number','A longer filename','A second network cable'],0,'TLS provides encryption and authenticated connection properties when configured and validated correctly. TCP alone does not.'),
    S('Why does a TCP application receiving 100 bytes in one write need a framing strategy when reading records?', 'A read may return part of a write or combine multiple writes. Use a length prefix, delimiter with escaping, or another explicit framing protocol, and loop until a complete record is assembled.'),
    C('An application sends numbered UDP updates 10,11,12 and receives 10,12,11. Propose a policy for a dashboard that only needs the latest complete state.', 'Track the newest accepted sequence and discard older updates, so 11 does not replace 12. Define sequence wraparound, reconnect/reset behavior, and how complete snapshots or missing updates are handled.'),
    C('A client retries a payment-like operation after a TCP connection breaks before it receives a reply. Why does reliable TCP not prove the operation failed, and what application design avoids duplicate actions?', 'The server may have committed the action before the response was lost. Use a durable idempotency key and return the recorded outcome on retries. Transport delivery and business-operation completion are separate concerns.'),
    D('Evaluate “UDP is always faster than TCP” as an interview claim. Identify the workload and network facts needed to compare them.', 'Compare latency, loss, message size, congestion, connection setup, ordering requirements, and application-level retries. UDP offers fewer transport guarantees, but recreating them can add cost. Define a measurable goal; no universal performance ranking follows from the protocol names.'),
    C('Design minimal application-level reliability for small UDP messages without claiming it is a complete TCP replacement.', 'Use message IDs, acknowledgments, bounded retries/timeouts, duplicate suppression, and explicit ordering requirements. Address congestion/backoff, packet size, and authentication if needed. Explain that network failure still prevents absolute delivery guarantees and retained state has costs.'),
  ]),
  'java-interview':B([
    M('Which design most directly supports encapsulation of an account balance?', ['A public mutable balance field','A private balance with validated deposit/withdraw methods','A global static balance for every account','A setter accepting every possible value'],1,'Encapsulation controls how state changes so class invariants can be enforced.'),
    S('Explain abstraction versus encapsulation using a payment interface and a class’s private fields.', 'Abstraction exposes relevant operations, such as pay(), while hiding unnecessary implementation detail. Encapsulation controls access to internal state and behavior; private fields plus meaningful methods support it.'),
    S('What is method overriding, in one sentence?', 'A subclass provides an implementation of an inherited instance method with a compatible signature and return/access rules, allowing runtime dispatch.'),
    C('A class stores a private List<String> but returns that same mutable list from a getter. Explain why private alone has not protected its invariant.', 'Callers can mutate the returned list and change internal state indirectly. Return an appropriate defensive copy or read-only view, while considering whether elements themselves are mutable.'),
    M('A parent reference points to a child object, and both define the same overridden instance method. Which implementation is called?', ['Always the parent implementation','The child object’s overriding implementation','A random implementation','Neither; overriding prevents calls'],1,'Runtime polymorphism dispatches an overridden instance method based on the actual object class.'),
    S('Why might an immutable class expose getters but no setters? Name another condition needed beyond removing setters.', 'It prevents direct later field changes, but must also control mutable references through constructor/getter defensive handling, restrict subclass mutation paths, and initialize state correctly.'),
    C('A method needs “something that can send a notification,” currently email but later SMS. Compare depending on EmailSender directly with depending on a NotificationSender interface.', 'An interface expresses the required operation and lets implementations vary or be replaced in tests. Avoid a needless interface if there is no useful boundary; the important point is isolating behavior and dependencies.'),
    C('A subclass overrides withdraw(amount) but permits negative balances although the parent promises they never occur. Evaluate substitutability.', 'It breaks the parent’s behavioral contract for callers relying on nonnegative balances. Preserve the invariant, revise the common abstraction explicitly, or use a separate capability/type for overdraft behavior.'),
    D('Evaluate an inheritance hierarchy Car extends Engine. Propose a model and explain a case where inheritance would be appropriate.', 'A car has an engine, so composition is appropriate. Inheritance should reflect a substitutable is-a relationship, such as a specialized implementation of a shared interface, while honoring behavior contracts. Avoid using inheritance only to reuse fields.'),
    C('A supposedly thread-safe account has synchronized deposit and withdraw, but transfer calls them separately on two accounts. What correctness questions remain?', 'Separate synchronization does not make the whole transfer atomic; observers may see a partial transfer and failures may interrupt it. Define transfer invariants, coordinate locks in a fixed order or use a transactional design, and prevent deadlock.'),
  ]),
  sql:B([
    M('Which SQL clause filters individual rows before grouping?', ['ORDER BY','WHERE','GROUP BY','SELECT DISTINCT only'],1,'WHERE selects qualifying input rows; HAVING filters groups after aggregation.'),
    S('For Students(id,name,marks), write a query returning only name where marks is at least 80.', '`SELECT name FROM Students WHERE marks >= 80;` The inclusive comparison includes exactly 80.'),
    S('How do you test whether an optional email column is missing (SQL NULL)?', 'Use `WHERE email IS NULL`, not `email = NULL`. SQL null comparisons do not behave like ordinary equality.'),
    C('Students contains (1,Ana,80),(2,Ben,95),(3,Cara,80) as (id,name,marks). Return names sorted by marks descending, then name ascending.', '`SELECT name FROM Students ORDER BY marks DESC, name ASC;` Result: Ben, Ana, Cara. The second sort key makes score ties deterministic for these names.'),
    M('A table has three rows and values 10, NULL, 20 in column score. What do COUNT(*) and COUNT(score) return?', ['3 and 3','2 and 2','3 and 2','2 and 3'],2,'COUNT(*) counts rows; COUNT(score) counts nonnull score values.'),
    S('For Orders(customer_id,amount), return each customer_id and total amount only when their total exceeds 1000.', '`SELECT customer_id, SUM(amount) AS total FROM Orders GROUP BY customer_id HAVING SUM(amount) > 1000;` HAVING applies the condition to each aggregated group.'),
    C('Customers(id,name) has Ana(1) and Ben(2). Orders(id,customer_id) has orders 10 and 11, both for Ana. Explain INNER JOIN versus LEFT JOIN row counts from Customers to Orders.', 'The inner join returns 2 matching rows for Ana. The left join returns those plus a Ben row with null order fields, totaling 3. A join can produce several rows for one customer.'),
    C('Write a query listing customers with zero orders using Customers(id,name) and Orders(id,customer_id), where order id is nonnull.', '`SELECT c.id, c.name FROM Customers c LEFT JOIN Orders o ON o.customer_id = c.id WHERE o.id IS NULL;` A correlated NOT EXISTS query is also correct and avoids relying on a nullable non-key field.'),
    D('An application builds `SELECT * FROM Users WHERE name = ...` by concatenating raw user input. Evaluate escaping by replacing one quote and propose a stronger design.', 'Use a parameterized query with bound values so input is treated as data, not SQL syntax. Ad hoc escaping is fragile across dialects and contexts. Validate separately for business rules; identifiers require explicit allowlisting rather than value parameters.'),
    C('For Employees(id,name,salary), return all rows whose salary is the second-highest distinct nonnull salary, including ties. Avoid dialect-specific LIMIT syntax.', '`SELECT id, name, salary FROM Employees WHERE salary = (SELECT MAX(salary) FROM Employees WHERE salary < (SELECT MAX(salary) FROM Employees));` The nested maximum excludes the top distinct value. With fewer than two distinct salaries, no rows qualify.'),
  ]),
};
