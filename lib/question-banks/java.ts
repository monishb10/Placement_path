import {B,M,S,C,D} from './types';
export const javaQuestions = {
variables:B([
 M('Which Java type directly represents a true-or-false eligibility flag?',['int','boolean','char','double'],1,'boolean stores true or false; numeric and character types do not represent a Boolean flag directly.'),
 S('Choose appropriate Java types for a student name, an age in completed years, and 1.5 hours of practice.','Use String, int, and double respectively. A String holds text, int holds whole numbers, and double can represent a fractional quantity.'),
 S('What are the final values of `int a=4; int b=a; a=9;`? Explain the assignment.','a is 9 and b is 4. Assigning a primitive value copies that value; b does not remain linked to a.'),
 C('A program stores 3,000,000,000 visits in an int. Identify the type problem and give a valid declaration.','The value exceeds the int maximum of 2,147,483,647. Use `long visits=3_000_000_000L;`; the L makes the literal a long.'),
 M('What is the value of `double average=5/2;`?',['2.0','2.5','3.0','Compilation error'],0,'Both operands are int, so division produces 2 before widening to double. Use 5/2.0 to obtain 2.5.'),
 S('Predict the result of `int x=(int)7.9;` and explain why it differs from rounding.','x is 7. Casting this positive finite double to int discards the fractional part; it does not round to the nearest integer.'),
 C('A patient ID is "00127" and will never be used in arithmetic. Should it be stored as an integer or a String, and why?','Use String to preserve leading zeros and the identifier’s textual meaning. Validate its required format separately instead of converting it into a quantity.'),
 C('Daily event counts can each reach 2 billion, and you must total 30 days. Design the types and the boundary checks.','Read counts as long and accumulate into long; the maximum total is 60 billion, within long. Reject negative counts and values beyond the stated daily limit; test zero and maximum totals.'),
 C('A developer writes `long area=100000*100000;`. Explain the bug and give a safe correction.','The multiplication happens as int and overflows before assignment. Use `100000L*100000` or cast an operand to long before multiplication. A long destination alone does not widen earlier operations.'),
 D('Design the data representation for a billing total, a customer ID with leading zeros, and an optional delivery date. Explain precision, meaning, and missing values.','Use integer minor currency units or BigDecimal with an explicit rounding policy for money, String for the ID, and a date type with an explicit missing-value policy. Distinguish missing data from zero or an invented date; do not use binary floating point when exact decimal money is required.'),
]),
operators:B([
 M('What does `17 % 5` evaluate to?',['2','3','3.4','5'],0,'The remainder is 2 because 17 = 3×5 + 2.'),
 S('Evaluate `2 + 3 * 4` and state the rule involved.','The result is 14. Multiplication is evaluated before addition unless parentheses change the grouping.'),
 S('Write a Java expression that tests whether integer n is even, including negative integers.','Use `n % 2 == 0`. An integer divisible by 2 has zero remainder, whether positive, negative, or zero.'),
 C('You have 53 tokens and 8 tokens per full group. Calculate full groups and leftover tokens using Java operators.','53/8 gives 6 complete groups and 53%8 gives 5 leftovers. Integer division and remainder describe complementary parts of the grouping.'),
 M('After `int x=3; int y=x++ + 2;`, what are x and y?',['x=4, y=6','x=3, y=5','x=4, y=5','x=5, y=4'],2,'Post-increment contributes the old value 3 to the addition, giving y=5, then leaves x=4.'),
 S('Why is `denominator != 0 && numerator / denominator > 2` safe from division by zero in this condition?','&& evaluates the right operand only when the left is true. A zero denominator makes the left false and skips division; using & would evaluate both operands.'),
 C('Three marks total 251. A program prints 83.0 as their average. Explain the likely expression and correct it.','It likely uses integer division before assigning to double, such as `double avg=251/3;`. Use `251/3.0` or cast an operand; the average is approximately 83.6667.'),
 C('Given 0 ≤ n ≤ 1,000,000,000, derive a safe Java expression for 1+2+…+n and explain the overflow limits.','Use `n * (n + 1L) / 2`, ensuring multiplication is long. The product is at most 1,000,000,001,000,000,000, within long. The formula uses O(1) time and space for these bounds.'),
 C('A price is 199 rupees and 99 paise, and a discount is 10%. Design a calculation that makes the final rounding rule explicit.','Represent 19,999 paise as an integer or use BigDecimal. The unrounded discounted amount is 17,999.1 paise; select and document a rounding rule for whole paise, such as nearest paise giving 17,999. Test half-way cases rather than silently truncating.'),
 D('Review a Boolean expression combining age eligibility, consent, and an override. Consent is mandatory, even with an override. Give the expression and a truth-table test that catches a mistake.','Use `consent && (ageEligible || override)`. Test consent=false with override=true: access must remain false. This catches `(consent && ageEligible) || override`, which incorrectly bypasses mandatory consent.'),
]),
input:B([
 M('Which Scanner method reads the next complete line?',['nextInt()','next()','nextLine()','hasNextInt()'],2,'nextLine reads through the end of the current line. next reads one whitespace-delimited token, and hasNextInt only checks availability and type.'),
 S('Which import is needed for `Scanner`, and which standard stream normally supplies console input?','Import `java.util.Scanner` and construct it with `System.in`.'),
 S('Input contains `8 15 23`. What should three calls to nextInt return, in order?','They return 8, 15, and 23. Token-based integer reading treats separating whitespace as delimiters.'),
 C('Input is an age on one line and a full name on the next. Why can nextLine immediately after nextInt return an empty string? Give one correction.','nextInt leaves the line separator behind; nextLine consumes the remainder of that line. Consume that remainder once before reading the name, or read both lines and parse the age.'),
 M('A value may be 5,000,000,000. Which read operation is appropriate?',['nextInt()','nextLong()','nextBoolean()','nextByte()'],1,'The value exceeds int but fits in long, so nextLong is appropriate.'),
 S('A judge expects only `42`. Why can printing `The answer is 42` fail even though the number is correct?','The extra words violate the required output contract. Print only the specified answer and separators; debugging messages should not contaminate standard output.'),
 C('An input begins with n=3 and then contains `4 7 9`. Describe the parsing and two checks you would perform in a user-facing tool.','Read n, then exactly n values. Validate an acceptable n range and report missing or malformed values; distinguish user-input validation from an interview judge that guarantees valid input.'),
 C('A file contains millions of integers and Scanner parsing is a bottleneck. Propose an alternative without changing the algorithm’s meaning.','Use buffered input with a token parser or BufferedReader with suitable tokenization. Handle whitespace, signs, EOF, and numeric bounds explicitly; benchmark parsing separately from the algorithm. Faster parsing does not change an O(n²) algorithm into O(n).'),
 C('A line-based task allows an empty message. Explain how to distinguish an empty line from end-of-file with BufferedReader.','readLine returns an empty String for an empty line and null when no line remains. Test null separately; treating both as the same can incorrectly discard valid empty input.'),
 D('Design a resilient import flow for rows containing ID, date, and count. Some rows are malformed. Explain error reporting and whether to stop or continue.','Define the delimiter, escaping, types, and required fields. Report row number, field, and reason; choose documented all-or-nothing validation or partial import with rejected-row reporting. Do not silently shift columns or convert missing counts to zero.'),
]),
conditions:B([
 M('Which Java condition checks whether n is strictly positive?',['n >= 0','n != 0','n > 0','n < 0'],2,'n > 0 excludes both zero and negative values.'),
 S('Write conditions that classify an integer as positive, negative, or zero.','Use if(n>0), else if(n<0), else. The final branch handles zero after the other possibilities are excluded.'),
 S('For `if (stock < 5)`, does stock=5 enter the branch?','No. The comparison is strictly less than 5; use <= if the boundary must be included.'),
 C('Marks at least 90 earn A, at least 75 earn B, and lower marks earn C. Explain why checking >=75 before >=90 in an if/else-if chain is wrong.','A mark such as 95 satisfies the first condition and is assigned B. Check the stricter high threshold first, then the lower one.'),
 M('A person must be at least 18 and have consent. Which expression matches the rule?',['age>=18 || consent','age>18 && consent','age>=18 && consent','age<18 || !consent'],2,'Both requirements must hold, so use &&, and >= includes age 18.'),
 S('Give boundary tests for a valid score range of 0 through 100 inclusive.','Test −1, 0, 1, 99, 100, and 101. The endpoints should be accepted and the immediately adjacent outside values rejected.'),
 C('A discount rule applies to students or senior citizens, but only with a valid membership. Express the rule and explain parentheses.','Use `(student || senior) && member`. Parentheses group the two alternative eligibility categories before applying mandatory membership.'),
 C('A shipping policy offers free delivery at order total >=500, except remote locations always pay. Build a decision table and identify a boundary test.','Remote=true always implies a charge. Otherwise total>=500 is free and total<500 is charged. Test a remote order of 500 and a non-remote order of exactly 500 to verify the exception and boundary.'),
 C('Two separate if statements independently set status to approved or rejected, allowing a later condition to overwrite an earlier decision. How would you redesign this?','Specify precedence and mutually exclusive outcomes first. Use an if/else-if/else chain or return one decision from a dedicated method. Test combinations where multiple conditions are true rather than relying on assignment order.'),
 D('A form has ten validation rules and several cross-field dependencies. Compare one long Boolean expression with a set of named validation functions.','Named functions expose each rule and can return actionable errors. Combine their results under an explicit policy, keep cross-field checks visible, and test rules independently plus in combination. A single expression may be compact but can conceal precedence and poor error messages.'),
]),
loops:B([
 M('How many times does `for(int i=0;i<4;i++)` execute its body?',['3','4','5','Infinitely many'],1,'The body runs for i=0,1,2,3: four iterations.'),
 S('When is a while loop useful compared with a for loop?','Use while when repetition depends mainly on a condition rather than a known count, such as reading until EOF. Either construct can express many of the same algorithms.'),
 S('What is the difference between break and continue within a loop?','break exits the nearest loop; continue skips the remaining body of the current iteration and proceeds with the next iteration’s control steps.'),
 C('A loop intended to visit n array elements uses `i<=n` starting from zero. Explain the failure and correction.','It tries to access index n, which is outside an array of length n. Use i<n; valid indices are 0 through n−1.'),
 M('What does a running total become after adding i for i=1,2,3,4?',['4','6','10','16'],2,'The total is 1+2+3+4=10.'),
 S('Analyze a nested loop where the inner loop runs n times for each of n outer iterations. Assume constant work per inner iteration.','There are n² inner iterations, giving Θ(n²) time. If only counters and a running total are stored, auxiliary space is O(1).'),
 C('A countdown loop uses `while(n>0)` but never changes n. What is the bug, and what invariant or progress measure would you check?','For positive n it never terminates. Decrease n toward zero and check that it remains a nonnegative measure that strictly decreases on each iteration.'),
 C('An outer loop runs i from 0 to n−1 and the inner loop runs j from 0 to i−1. Derive the total number of iterations and the complexity.','The total is 0+1+…+(n−1)=n(n−1)/2, so time is Θ(n²), not Θ(n). The shrinking inner range changes the constant factor but not the growth order.'),
 C('A loop repeatedly doubles i from 1 while i<=n. Explain the iteration count and an overflow-related termination risk.','The count is floor(log2 n)+1 for n>=1. Fixed-width integer overflow can wrap i and break the intended progress; use a safe wider bound or guard the multiplication before doubling.'),
 D('You need to process a million records but stop on a fatal error while retaining diagnostics for earlier rows. Design the loop’s responsibilities.','Separate parsing, validation, processing, and result recording. Define which errors are fatal, record the row and cause, and stop before applying later records. Explain whether earlier writes are committed or rolled back; loop control alone does not define transaction behavior.'),
]),
arrays:B([
 M('What is the first valid index of a nonempty Java array?',['−1','0','1','Its length'],1,'Java arrays use zero-based indexing.'),
 S('What are the length and last valid index of `int[] a={4,8,2};`?','The length is 3 and the last valid index is 2.'),
 S('How do you access the second element of a nonempty array with at least two elements?','Use a[1]. Position two corresponds to index one.'),
 C('A maximum-search algorithm initializes max=0. Explain its result on [−8,−3,−6] and fix the initialization.','It incorrectly returns 0, which is absent. For a nonempty array initialize max=a[0], then scan the remaining values; the correct answer is −3.'),
 M('What is the worst-case time to find an arbitrary target by scanning an unsorted array of n elements?',['O(1)','O(log n)','O(n)','O(n²)'],2,'The target may be last or absent, requiring up to n comparisons.'),
 S('Describe an in-place reversal of [1,2,3,4,5], including the final array and auxiliary space.','Swap the first and last elements, then move inward until the pointers meet. The result is [5,4,3,2,1]; time is O(n) and auxiliary space is O(1).'),
 C('An average function receives an empty array. What contract should be chosen instead of blindly dividing by length?','State whether empty input is invalid, returns an optional result, or throws a documented exception. There is no arithmetic mean for zero observations; silently returning zero confuses missing data with a real average.'),
 C('Find the second-largest distinct value using one pass. Explain what happens for [5,5,3] and for [5,5].','Maintain the largest and second-largest distinct candidates with explicit availability flags or appropriate sentinels. Ignore values equal to the maximum. The first array yields 3; the second has no second distinct value. Time is O(n), auxiliary space O(1).'),
 C('Two arrays a and b must remain independent, but the developer writes `int[] b=a;`. Explain the behavior and a correction.','Both references point to the same array, so editing b also changes the object observed through a. Use a.clone or Arrays.copyOf for a separate primitive array. For nested arrays, cloning only the outer array still shares inner arrays.'),
 D('A sensor sends readings continuously, but only the most recent 100 values are needed. Design a fixed-array solution and explain wraparound.','Use a circular buffer with a write index and count. Replace the oldest slot when full and advance the index modulo 100. Distinguish logical time order from physical array order; insertion uses O(1) time and storage remains fixed.'),
]),
strings:B([
 M('Which operation compares the text content of two non-null Java Strings?',['a == b','a.equals(b)','a = b','a.compareReference(b)'],1,'equals compares content; == tests whether the references identify the same object.'),
 S('What does String immutability mean?','A String object’s character sequence cannot be changed after creation. Operations that appear to modify text return a new String or another result.'),
 S('For `String s="Java"`, what are s.length() and s.charAt(0)?','The length is 4 and charAt(0) is J.'),
 C('Two independently created Strings both contain "java", but == is false. Explain how both facts can be true.','Different objects can hold equal content. Their references differ, while equals returns true for the shared text.'),
 M('What text is returned by `"placement".substring(0,5)`?',['place','placem','lacem','placement'],0,'The start index is included and end index excluded, so indices 0 through 4 produce place.'),
 S('Describe an O(n)-time test for an ASCII palindrome using O(1) auxiliary space.','Compare characters from opposite ends with two pointers and move inward. Stop on a mismatch. State separately whether spaces and case should be ignored, since that changes the contract.'),
 C('Building a long report uses repeated `result = result + nextPart` inside a loop. Why consider StringBuilder?','Repeated concatenation can copy the growing prefix many times and produce quadratic total work. StringBuilder maintains a mutable buffer and usually builds the result in linear total time relative to the appended text.'),
 C('An anagram checker must ignore spaces and letter case for English letters. Design it and explain why equal lengths alone are insufficient.','Normalize case and skip spaces, then compare counts of each letter. Equal normalized lengths are necessary but do not guarantee matching frequencies. An array of 26 counters gives O(n+m) time and fixed auxiliary space under the English-letter assumption.'),
 C('A username rule limits visible characters. Why can String.length() be an inadequate count for arbitrary Unicode text?','length counts UTF-16 code units. Some code points use two units, and a user-perceived character can contain multiple code points. Specify whether the rule counts code units, code points, or grapheme clusters and use matching validation.'),
 D('Design a log redactor that removes a known secret token while preserving the rest of each line. Discuss literal matching, empty tokens, and tests.','Treat the token as literal text unless regular-expression behavior is explicitly desired. Reject or define empty-token behavior, and test adjacent and repeated matches plus lines without the token. Define a policy for overlapping matches and verify that complete secret tokens are not exposed.'),
]),
methods:B([
 M('Which keyword sends a value back from a non-void Java method?',['break','return','continue','new'],1,'return ends the method and supplies its result to the caller.'),
 S('Identify the parameter and return type in `static int square(int value)`.','value is an int parameter, and the return type is int.'),
 S('What is a method with return type void allowed to return?','It returns no value. It may use a bare return to exit early or finish normally.'),
 C('A helper receives `int n`, assigns n=10, and returns void. The caller passed a variable containing 3. What is the caller’s value afterward?','It remains 3. Java passes the primitive value into a separate parameter variable.'),
 M('Which pair is valid method overloading in one class?',['sum(int) and sum(int) with different return types','sum(int) and sum(double)','Two identical sum(int) declarations','sum(int) and another sum(int) differing only in parameter name'],1,'Overloading requires a different parameter list. Return type or parameter names alone cannot distinguish overloads.'),
 S('Define a contract for isEven(int n), including zero and negative inputs.','Return true exactly when n%2==0. Zero and negative even integers are even; the method should have no printing or mutation side effects.'),
 C('A method both computes a total and prints a report. Explain a refactoring that makes testing easier.','Extract a method that returns the total from input values, and keep formatting/output in a separate method. Tests can then compare returned values without capturing console output.'),
 C('A method receives an array reference. It changes a[0], then assigns its parameter to a new array. Which change can the caller observe, and why?','The caller observes the modification to the original array’s element. Reassigning the local parameter does not replace the caller’s reference. Java passes the reference value by value.'),
 C('Design a safe divide method whose denominator may be zero. Compare a sentinel, an exception, and an optional result.','A sentinel can be ambiguous if it is also a valid result. An exception suits an invalid contract; an optional result can model expected absence. Document the policy and ensure callers handle it rather than silently producing a misleading answer.'),
 D('An interview solution repeats the same parsing, validation, and search logic in three places. How would you divide responsibilities without creating excessive abstraction?','Extract meaningful reusable methods with clear inputs and outputs, such as parseInput and search. Keep one-purpose validation near its contract, avoid hidden shared state, and retain a short main flow. Judge the design by readability, testability, and whether the abstractions actually remove duplication.'),
]),
oop:B([
 M('Which principle protects an object’s state through controlled operations?',['Encapsulation','Recursion','Sorting','Hashing'],0,'Encapsulation keeps state and the operations that enforce its rules together behind a controlled interface.'),
 S('Explain the difference between a class and an object using a bank account example.','A class defines the account’s structure and behavior; an object is one account instance with its own balance and identity.'),
 S('What is the role of a constructor?','A constructor initializes a newly created object, ideally establishing the invariants required for valid use.'),
 C('A BankAccount has a public balance field. Explain a problem and redesign the interface to reject invalid withdrawals.','Anyone can set an invalid balance directly. Make it private and expose a withdraw method that checks a positive amount and sufficient funds before changing state.'),
 M('A variable of type Animal refers to a Dog that overrides speak(). Which implementation runs when speak is invoked?',['Always Animal.speak','Dog.speak for the overridden instance method','Both methods automatically','Neither without a cast'],1,'Dynamic dispatch chooses the overridden instance method from the object’s runtime type.'),
 S('Compare method overloading and overriding in one sentence each.','Overloading uses the same method name with different parameter lists. Overriding supplies a subclass implementation of an inherited instance method with a compatible signature.'),
 C('A Car needs an Engine but is not a kind of Engine. Should the model use inheritance or composition? Explain.','Use composition: a Car has an Engine. Inheritance would incorrectly claim a Car is substitutable for an Engine and can create an unsuitable interface.'),
 C('Design a payment service that supports card and wallet payments without a large condition chain throughout the application.','Define a PaymentMethod interface and separate implementations. Keep shared orchestration in a service that depends on the interface, and put method-specific validation in implementations. Discuss error handling and tests with a fake implementation.'),
 C('A getter returns an internal mutable list of account transactions. Why might private fields still fail to protect the account?','The caller can mutate the returned list and bypass invariants. Return an immutable snapshot or a controlled read-only view, and consider whether contained transaction objects are also mutable.'),
 D('Evaluate whether every shared field between two classes justifies a common superclass. Give a criterion and a counterexample.','A superclass should reflect a valid substitutable behavior contract, not merely reduce duplicate fields. Student and Invoice may both have an ID but are not meaningfully interchangeable. Prefer composition or a small interface when it captures an actual shared capability.'),
]),
arraylist:B([
 M('Which property distinguishes ArrayList from a Java array?',['It can resize as elements are added','It uses only primitive values','Its first index is 1','It never stores duplicates'],0,'ArrayList manages a resizable backing array; ordinary arrays have fixed length.'),
 S('How do you append 7 to an ArrayList<Integer> and read its first element?','Use list.add(7) and list.get(0), assuming the first element exists.'),
 S('What is the difference between an ArrayList’s size and its backing capacity?','Size is the number of stored elements. Capacity is the amount of allocated backing storage and may be larger than size.'),
 C('An ArrayList contains [1,2,3,4]. You remove matching elements while increasing an index after every removal. Why might a match be skipped?','Removal shifts later elements left. Advancing the index immediately skips the element that moved into the removed position. Use removeIf, an iterator’s remove, or a carefully managed index.'),
 M('Which describes appending to an ArrayList across many operations?',['Always O(n²)','Amortized O(1)','Always O(log n)','Worst-case O(1) for every append'],1,'Most appends are constant-time, but occasional resizing copies elements. The cost averaged over a sequence is amortized O(1).'),
 S('Explain why inserting at index 0 of a large ArrayList is O(n).','Existing elements must shift one position to make room, so work grows with the number of stored elements.'),
 C('You have `ArrayList<Integer> a=new ArrayList<>(List.of(10,20,30));`. What does `a.remove(1)` remove, and how can you remove the value 20 explicitly?','remove(1) selects the index overload and removes 20 at index 1. To express removal by value, use remove(Integer.valueOf(20)); overload choice matters even when this example has the same outcome.'),
 C('You repeatedly prepend 100,000 events and then iterate once. Evaluate ArrayList against ArrayDeque for this workload.','Repeated prepends to ArrayList incur shifting and can total O(n²). ArrayDeque supports efficient additions at the front and iteration; use it if indexed random access is not required. Choose based on actual operations rather than name alone.'),
 C('An ArrayList is copied using `new ArrayList<>(original)`, but editing an object inside the copy affects the original. Explain the copy depth.','The list structure is copied, while element references are shared. Independent mutable elements require copying those objects too, or using immutable elements.'),
 D('A UI shows a filtered list while the source list changes. Design a clear ownership policy and explain snapshot versus live-view behavior.','Choose either a snapshot that changes only on refresh or a coordinated view that reflects source updates. Avoid ambiguous shared mutation, document which component owns edits, and test deletions and concurrent refreshes. A collection choice alone does not supply thread safety.'),
]),
hashmap:B([
 M('What does a HashMap associate with each unique key?',['Exactly one current value','A mandatory sorted position','Only another key','A fixed array index visible to callers'],0,'A map associates a key with one current value; inserting the same key again replaces that mapping’s value.'),
 S('What does getOrDefault("java",0) return when the key is absent?','It returns 0. It does not add the key to the map.'),
 S('After put("A",1) and put("A",2), how many mappings exist and what is A’s value?','There is one mapping, and A maps to 2.'),
 C('Count word frequencies in [java,sql,java,java]. Describe the update and the final map.','For each word, replace its count with getOrDefault(word,0)+1. The final frequencies are java→3 and sql→1.'),
 M('Which assumption supports average O(1) HashMap lookup in interview analysis?',['Keys are already sorted','Hash distribution is reasonable','Values are unique','The map never resizes'],1,'Reasonable hashing and load behavior support expected constant-time lookup. It is not an unconditional worst-case promise.'),
 S('Why must equal keys have equal hash codes?','The map uses hashes to narrow where it looks, then equality to match keys. Equal objects with inconsistent hashes can be searched in different locations and violate the expected key contract.'),
 C('You need a frequency report in alphabetical key order. Why should you not depend on HashMap iteration order?','HashMap does not guarantee that ordering. Sort the keys before reporting or use an ordered map whose contract matches the requirement, such as TreeMap for natural key ordering.'),
 C('A mutable Student object is used as a key, with name included in equals and hashCode. Its name changes after insertion. Diagnose a failed lookup.','Changing hash-relevant state breaks the assumption that the key remains in the appropriate bucket. Prefer immutable key fields or a stable ID; remove and reinsert under controlled conditions if a key must change.'),
 C('Design a one-pass two-sum algorithm that returns two distinct indices. Explain when to insert the current value.','Before inserting the current value, look for its complement among earlier elements. If found, return the stored index and current index; otherwise insert the current value. This prevents using one element twice and gives expected O(n) time with O(n) space.'),
 D('A shared request counter is updated by multiple threads using get then put. Explain why HashMap choice and update atomicity are separate concerns.','The read-modify-write can lose updates, and ordinary HashMap is not a concurrent collection. Use a suitable concurrent map with an atomic merge/compute operation or another synchronized counter design. Merely replacing the container without fixing the compound operation may still be wrong.'),
]),
hashset:B([
 M('What is the defining property of a Set?',['Sorted iteration for every implementation','Unique elements according to equality','Integer-only storage','Constant memory regardless of input'],1,'Sets reject duplicate elements according to their equality contract; ordering depends on implementation.'),
 S('After adding 4, 4, and 7 to a HashSet<Integer>, what is its size?','The size is 2 because the duplicate 4 is stored only once.'),
 S('Which HashSet operation checks whether an ID is present?','Use contains(id). It returns a boolean without adding the ID.'),
 C('Find whether [3,8,3,9] contains a duplicate while scanning once. Explain the stopping condition.','Insert each value into a set; add returns false when an equal value already exists. The second 3 reveals a duplicate, so the scan can stop there.'),
 M('Which operation preserves only elements common to two sets?',['Union','Intersection','Difference','Concatenation'],1,'Intersection contains exactly the elements present in both sets.'),
 S('Analyze duplicate detection using a HashSet for n values, under average hashing assumptions.','Expected time is O(n), and auxiliary space is O(n) in the all-distinct case.'),
 C('A UI must display unique visitor IDs in first-seen order. Is plain HashSet sufficient?','It enforces uniqueness but does not guarantee insertion order. Use LinkedHashSet or maintain explicit order separately.'),
 C('Two custom objects represent the same visitor ID but are treated as distinct in a set. What contract should be inspected?','Inspect equals and hashCode and define equality using the stable visitor identity. Both methods must agree; object identity alone will treat separate instances as distinct unless equality is overridden appropriately.'),
 C('You must detect repeated integer IDs from 0 through 999 using very little predictable memory. Compare HashSet with a boolean array.','A boolean array indexed by ID provides direct membership checks with a fixed 1,000-slot domain. HashSet is more flexible for sparse or unbounded IDs but has object and hashing overhead. Validate the ID range before indexing.'),
 D('A service must remember whether any of billions of events has appeared before. Explain why an in-memory HashSet may be unsuitable and how accuracy requirements shape alternatives.','An exact set requires storage proportional to distinct events, so persistence, partitioning, or retention windows may be necessary. Approximate membership structures can reduce memory but may introduce false positives; they are unsuitable if every duplicate decision must be exact without a secondary check.'),
]),
collections:B([
 M('Which interface most directly models a first-in-first-out waiting line?',['Set','Map','Queue','Comparator'],2,'Queue models retrieval according to queue ordering; a FIFO implementation serves the earliest inserted waiting item first.'),
 S('Choose between List, Set, and Map for an ordered log, unique IDs, and ID-to-name lookup.','Use List for the log, Set for unique IDs, and Map for ID-to-name associations.'),
 S('Why declare `List<Integer> values=new ArrayList<>();` rather than using ArrayList everywhere in an API?','The interface states the required list behavior while reducing dependence on one implementation. Implementation-specific behavior should be used only when the API actually requires it.'),
 C('A scheduler needs to repeatedly retrieve the smallest deadline. Compare a plain FIFO queue with PriorityQueue.','A FIFO queue orders by arrival rather than deadline. A PriorityQueue can expose the smallest deadline using its ordering rule, with O(log n) insertion/removal and O(1) peek.'),
 M('Which pair provides unique values in sorted order and key-value lookup in insertion order, respectively?',['TreeSet and LinkedHashMap','HashSet and TreeMap','ArrayList and HashMap','PriorityQueue and HashSet'],0,'TreeSet maintains sorted unique elements, while LinkedHashMap’s ordinary insertion-order mode preserves mapping insertion order.'),
 S('A list of records is sorted by score descending, then name ascending. Describe the comparator’s two decisions.','Compare scores in reverse order; when scores tie, compare names in ascending order. Use safe comparison methods rather than subtracting integers, which can overflow.'),
 C('A fixed list from Arrays.asList permits set but throws when adding an element. Explain the distinction.','It is a fixed-size view backed by the array, not a generally resizable ArrayList. Replacing an existing element is allowed, but changing size is not; copy into a new ArrayList for structural edits.'),
 C('Design a structure for an event feed requiring arrival order, duplicate rejection, and fast ID lookup. Explain one combined design.','Use an ordered sequence of events plus a map or set of seen IDs, updating them consistently, or an insertion-ordered map keyed by ID if one event per ID fits the contract. Define whether duplicate arrivals replace, ignore, or reject existing events.'),
 C('A sort comparator returns a-b for arbitrary int values. Give an overflow counterexample and correction.','With a=Integer.MAX_VALUE and b=−1, subtraction overflows and can reverse the ordering. Use Integer.compare(a,b), which compares without unsafe subtraction.'),
 D('Evaluate a collection choice for a workload with frequent random reads, occasional appends, and almost no front insertions. What evidence would change your choice?','ArrayList is a strong initial fit because indexed reads are O(1) and appends amortized O(1). Frequent front removals, stable iterator requirements, memory constraints, or concurrency needs may justify a different structure. Measure the actual operation mix and data sizes.'),
]),
complexity:B([
 M('Which growth rate describes one complete scan of n elements with constant work per element?',['O(1)','O(log n)','O(n)','O(n²)'],2,'The number of operations grows proportionally with n.'),
 S('What does auxiliary space measure?','It measures extra memory used by the algorithm beyond the input representation, with output storage usually stated separately according to the analysis convention.'),
 S('If input size doubles, how does the leading work of an n² algorithm change?','It grows by about a factor of four because (2n)²=4n², ignoring lower-order terms.'),
 C('One loop scans n values and a second separate loop scans n values. Explain why the total is not O(n²).','The loops are sequential, so work adds: n+n=2n, which is O(n). Multiplication applies when one loop’s full work is repeated inside another.'),
 M('A search halves its remaining range after each step. Which bound best matches the number of steps?',['O(log n)','O(n)','O(n log n)','O(n²)'],0,'Repeated halving reaches a constant-size range after logarithmically many steps.'),
 S('Analyze a recursive countdown with n calls, each doing constant work, including stack space.','Time is O(n), and call-stack auxiliary space is O(n). Small local variables do not make the total recursion space O(1).'),
 C('Algorithm A needs 100n operations and B needs n². Which has better asymptotic growth, and can B still be faster for small n?','A grows linearly and eventually dominates B favorably. B has fewer counted operations when 0<n<100 in this simplified model; asymptotic notation does not remove constant-factor effects at small inputs.'),
 C('An outer loop runs n times, and an inner loop doubles its counter from 1 up to n. Derive time and auxiliary space.','The inner loop runs O(log n) times per outer iteration, giving O(n log n) time. With only counters and no recursion or extra storage, auxiliary space is O(1).'),
 C('A solution sorts n items and then scans them once. Explain the overall bound and why the sort’s space must be identified separately.','For an O(n log n) sort, total time remains O(n log n) after adding O(n). Auxiliary space depends on the chosen sorting algorithm and implementation; it cannot be inferred just from the time bound.'),
 D('A candidate says an algorithm is optimal because it passed all small samples quickly. Evaluate the claim and propose better evidence.','Samples establish only limited correctness observations and timings. Derive operation and memory growth, test boundary and adversarial cases, and compare with a justified lower bound or best-known approach under the problem’s assumptions. Fast sample execution alone proves neither correctness nor optimality.'),
]),
};
