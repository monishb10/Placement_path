import {B,M,S,C,D,type Question} from './types';

export const structureQuestions: Record<string,Question[]> = {
  'linked-list':B([
    M('A node in a basic singly linked list stores:', ['Only an array index','A value and a reference to the next node','References to every other node','Only the list length'],1,'A node holds data and a next reference; the last node normally points to null.'),
    S('How is an empty singly linked list usually represented by its head reference?', 'head is null. Code must check this before accessing head.data or head.next.'),
    S('Why does reading the element at index k usually take O(k) time in a singly linked list?', 'There is no direct indexed address calculation. Follow next references from the head until reaching index k.'),
    C('Insert value 5 at the head of 10 → 20. Describe the pointer assignments and resulting list.', 'Create node 5, set its next to the old head, then set head to the new node. Result 5 → 10 → 20, O(1) time.'),
    M('Given a reference to the preceding node, deleting its nonnull next node in a singly linked list takes:', ['O(1) time','O(n) time always','O(log n) time','O(n²) time'],0,'Set prev.next=prev.next.next. This excludes time to locate prev and may need tail bookkeeping.'),
    S('Reverse 1 → 2 → 3 iteratively. Which references must you preserve at each step?', 'Maintain previous, current, and saved next. Save current.next before reversing the link, then advance. Result 3 → 2 → 1; O(n) time, O(1) auxiliary space.'),
    C('Find the middle of a list with slow and fast pointers. For four nodes, specify which middle your chosen loop returns.', 'Start both at head; advance slow once and fast twice while fast and fast.next are nonnull. It returns the second middle, node 3, for four nodes. State this even-length convention explicitly.'),
    D('Explain why Floyd’s slow/fast method detects a cycle without extra node storage, and how to find the cycle entry after a meeting.', 'Inside the cycle, the speed difference closes the offset modulo cycle length. After meeting, reset one pointer to head and move both one step; they meet at entry. Explain the distance relation from head and around the cycle; O(n) time, O(1) space.'),
    C('Two acyclic singly linked lists share some actual tail nodes. Design an O(1)-space algorithm to find the first shared node, and explain why equal values are insufficient.', 'Switch each pointer to the other list’s head after reaching null. They traverse equal combined lengths and meet at the shared reference or null. Compare node identity, not data; equal-valued separate nodes are not an intersection.'),
    C('Design a least-recently-used cache with O(1) expected get and put. Why does a singly linked list alone not meet the target?', 'Combine a key-to-node hash map with a doubly linked recency list. Move hits to the front; evict the tail on capacity overflow. The map locates nodes and prev/next links permit constant-time removal; handle capacity zero and updates to existing keys.'),
  ]),
  stack:B([
    M('A stack removes elements in which order?', ['First in, first out','Last in, first out','Smallest first','Random order'],1,'LIFO means the most recently pushed element is the next one popped.'),
    S('After push(2), push(5), pop(), push(7), what is the stack top?', '7. The pop removes 5; pushing 7 places it above 2.'),
    S('What is the difference between peek and pop, and what must callers consider for an empty stack?', 'Both inspect the top, but pop also removes it. Empty behavior depends on the chosen API; check emptiness or deliberately handle its null/exception contract.'),
    C('Use a stack to validate "([])" and "([)]". Explain the decisive check.', 'Push opening brackets; on a closing bracket, require a nonempty stack with the matching opener on top. The first string is valid; the second fails when ) encounters [.'),
    M('If every input bracket is pushed or popped at most once, validation of length n costs:', ['O(n²) time, O(1) space','O(n) time, O(n) worst-case space','O(log n) time, O(n) space','O(1) time, O(n) space'],1,'The scan is linear. A string of opening brackets can fill the stack to n entries.'),
    S('Evaluate the postfix expression "5 2 - 3 *". Why does operand pop order matter?', 'The result is 9: (5−2)×3. Pop the right operand first, then the left; reversing them changes subtraction and division.'),
    C('Find the next strictly greater value to the right of each element in [2,1,2,4,3], using −1 for none.', '[4,2,4,−1,−1]. A monotone stack of unresolved indices lets each new value resolve smaller previous values; equal values are not strictly greater.'),
    C('Design a stack that also returns the current minimum in O(1) time. Ensure duplicate minima are handled.', 'Store each pushed value together with the minimum at that depth, or keep a synchronized minimum stack including duplicates. Pop both records together. All operations are O(1), with O(n) space.'),
    D('A monotone-stack algorithm has a while loop inside a for loop. Explain how it can still have O(n) total time.', 'Use aggregate analysis: each index is pushed once and can be popped once. Across every inner loop there are at most n pops, so total operations are linear despite nesting.'),
    C('Build undo/redo for a text editor using stacks. What happens when a new edit occurs after several undo operations?', 'Keep undo and redo histories with reversible commands or snapshots. Undo transfers an action to redo; redo reapplies and returns it. A new edit normally clears redo because the previous future branch is no longer valid. Discuss snapshot memory versus inverse-command complexity.'),
  ]),
  queue:B([
    M('A conventional queue serves items in which order?', ['Last in, first out','Highest value first','First in, first out','Alphabetical order'],2,'FIFO serves the earliest enqueued item that has not yet been removed.'),
    S('Enqueue A, enqueue B, dequeue once, then enqueue C. What will the next dequeue return?', 'B. A was removed; B is now at the front ahead of C.'),
    S('How does a deque differ from an ordinary FIFO queue?', 'A deque permits insertion and removal at both ends. It can support queue-like or stack-like use.'),
    C('A circular queue has capacity 4 and front index 3. After removing one item, what is the new front index and why?', '0, using (front+1)%capacity. Wraparound reuses positions at the beginning of the backing array.'),
    M('Which traversal typically uses a FIFO queue to visit vertices by distance in an unweighted graph?', ['Depth-first search','Breadth-first search','Insertion sort','Binary search'],1,'BFS processes all vertices at one edge-distance before the next, assuming visited handling is correct.'),
    S('Compare poll() and remove() on an empty Java Queue.', 'poll() returns null; remove() throws NoSuchElementException. Choose the contract deliberately, and avoid using null as an ordinary queue element.'),
    C('Why does implementing every dequeue with ArrayList.remove(0) become inefficient for a large queue? Suggest a Java alternative.', 'Removing index 0 shifts remaining elements, taking O(n). ArrayDeque provides amortized O(1) operations at the ends for normal in-memory queue use.'),
    C('Implement a queue using two stacks. Explain when to transfer elements and give amortized and worst-case costs for a dequeue.', 'Push new items onto an input stack. Only when the output stack is empty, transfer all input items to it, reversing order. Each item transfers once: amortized O(1) per operation, but an individual dequeue can take O(n).'),
    D('A producer continuously adds jobs faster than workers complete them. Evaluate an unbounded queue and propose a bounded-queue policy.', 'An unbounded queue can exhaust memory and increase latency. Bound capacity and choose blocking, rejection, dropping, or spillover according to requirements. Define retries, cancellation, ordering, and queue-depth monitoring; a queue alone cannot solve insufficient processing capacity.'),
    C('Design a round-robin dispatcher with time quantum 2 for jobs A(3 units), B(1), C(2), all available at time 0. Give service order and completion times, ignoring switching overhead.', 'Run A from 0–2 and requeue its remaining 1; B from 2–3 completes; C from 3–5 completes; A from 5–6 completes. A FIFO ready queue provides rotation; arrivals and completion/removal rules must be explicit.'),
  ]),
  recursion:B([
    M('What stops a correctly defined recursive computation?', ['A larger input every time','A reachable base case','A global variable in every program','A loop is mandatory'],1,'Recursive calls must progress toward a base case that returns without further recursion.'),
    S('State a base case and recurrence for factorial of a nonnegative integer n.', 'factorial(0)=1; factorial(n)=n×factorial(n−1) for n>0. Numeric overflow remains a separate concern.'),
    S('A function prints n, then calls itself with n−1 until n=0, when it returns. What does it print for n=3?', '3, 2, 1. The print occurs before the recursive call; the base case prints nothing.'),
    C('A recursive sum computes sum(n)=n+sum(n−1) with sum(0)=0. Give time and auxiliary space, including the call stack.', 'O(n) time and O(n) stack space. Each call does constant extra work, but n unfinished calls can remain active.'),
    M('If a recursive function calls itself twice on n−1 without memoization, its call count usually grows:', ['Constantly','Linearly','Exponentially','Logarithmically'],2,'The recursion tree roughly doubles per level. Repeating identical subproblems does not avoid their work unless results are cached.'),
    S('Why can a recursive method with a valid mathematical base case still cause StackOverflowError?', 'The input may require too many simultaneous calls for the available stack. The base case may be reachable yet too deep; iterative processing or explicit data structures can help.'),
    C('In recursive array reversal, what base condition and recursive interval prevent both unnecessary work and out-of-bounds access?', 'Stop when left≥right. Otherwise swap valid endpoints and recurse on left+1,right−1. Handle an empty array with bounds 0,−1; time O(n), recursive space O(n).'),
    D('Prove termination and correctness for recursive binary search over an inclusive interval.', 'For termination, each recursive branch strictly reduces interval length until low>high. For correctness, sorted order excludes the discarded half; a match is valid, and the empty interval proves absence. Include duplicate-return semantics and midpoint safety.'),
    C('A method generates subsets using a shared mutable list. It adds a value, explores one branch, and then explores the exclusion branch. What must happen between branches and when recording a result?', 'Undo the addition before exploring exclusion, and copy the working list when saving a subset. Otherwise branches contaminate one another or stored results share the same mutated object. Enumerating all subsets requires exponential output work.'),
    C('A traversal of a chain with 100,000 nodes overflows Java’s call stack. Redesign it iteratively while preserving a recursive preorder visitation order.', 'Use an explicit stack of pending nodes. Pop and visit, then push children in reverse desired visit order. The heap-based stack avoids deep method calls; still account for O(n) worst-case explicit storage and validate identical visitation semantics.'),
  ]),
  trees:B([
    M('In a rooted tree, the root is the node that:', ['Has no children','Has no parent','Has exactly two children','Has the smallest value'],1,'The root has no parent. A leaf has no children; values need not be ordered.'),
    S('What distinguishes a binary tree from a general rooted tree?', 'Each binary-tree node has at most two children, usually called left and right. A general rooted tree may have more.'),
    S('Using edge count as depth, what are the depths of a root and its direct child?', 'The root has depth 0; a direct child has depth 1.'),
    C('A root has two children, and its left child has one child. Find the number of nodes, edges, and leaves.', '4 nodes, 3 edges, 2 leaves. The right child and the left grandchild are leaves.'),
    M('How many edges does a nonempty tree with n nodes have?', ['n','n−1','n+1','2n'],1,'A connected acyclic tree has exactly one fewer edge than nodes.'),
    S('With height measured in edges, compare the height of a 7-node chain and a perfect 7-node binary tree.', 'The chain has height 6; the perfect tree has height 2. Shape strongly affects recursive depth and tree-operation costs.'),
    C('Compute the height of a binary tree recursively. State a consistent convention for an empty tree and a leaf.', 'Choose height(null)=−1, so a leaf has height 0. Then height(node)=1+max(height(left),height(right)). O(n) time and O(h) recursive stack space.'),
    C('A function recomputes subtree heights at every node when checking balance. Explain its worst-case cost and design an improvement.', 'On a chain, repeated height scans can cost O(n²). A postorder helper returns height or an unbalanced sentinel, combining results once per node. This gives O(n) time and O(h) stack space.'),
    D('Design an unambiguous serialization for a binary tree that may contain duplicate values. Explain why traversal values alone may be insufficient.', 'Use preorder with explicit null markers and a parseable value format. A decoder consumes a node followed by its two subtrees. Without shape/null information, identical value sequences can represent different trees; escaping or length-prefixing is needed for arbitrary text values.'),
    C('Find the diameter of a binary tree, defined as the maximum number of edges on any simple path. Explain why the path need not pass through the root.', 'At each node combine left and right heights as leftHeight+rightHeight+2, using null height −1, while tracking the best anywhere. Return one-side height upward. A longest path may lie wholly in a subtree; O(n) time, O(h) stack.'),
  ]),
  bst:B([
    M('In a BST with distinct keys, every value in a node’s left subtree must be:', ['Less than the node’s key','Greater than the node’s key','Equal to the parent key','Less than only its sibling'],0,'The ordering applies to the entire subtree, not only the immediate child.'),
    S('Insert 6 into a BST with root 8 and left child 3, where 3 has no children. Where does 6 go?', 'As the right child of 3: 6<8 sends it left, then 6>3 sends it right.'),
    S('What order does inorder traversal produce for a BST containing distinct keys?', 'Strictly increasing key order, because every left subtree precedes its root and every right subtree follows it.'),
    C('A BST receives sorted keys 1,2,3,4,5 without balancing. What shape and worst-case search time result?', 'A right-leaning chain with height 4. Search may visit all n nodes, so worst-case time is O(n), not automatically O(log n).'),
    M('Where is the smallest key in a nonempty BST?', ['The root always','The rightmost node','The leftmost node','Any leaf'],2,'Repeatedly follow left references until no left child remains.'),
    S('Describe deletion of a BST node that has two children.', 'Replace its key/data with its inorder successor (minimum in right subtree) or predecessor, then delete that replacement node from its original location. Preserve any associated record and the duplicate-key policy.'),
    C('Root 10 has left child 5, and 5 has right child 12. Local parent-child comparisons look valid. Is the whole tree a BST?', 'No. The 12 lies in the left subtree of 10 and violates its ancestor bound. Validate using inherited lower/upper bounds or a globally ordered inorder traversal.'),
    C('Find the kth smallest key in a distinct-key BST. Compare a one-off traversal with an augmented tree for many such queries.', 'A one-off inorder traversal can stop at k, using O(h+k) time and O(h) stack. Subtree-size metadata supports O(h) selection per query, but insertions/deletions must update sizes; balancing is needed for guaranteed logarithmic height.'),
    D('Design a duplicate-key policy for a BST storing word frequencies. Compare storing repeated nodes with one node per word plus a count.', 'One node per unique word keeps search ordering simple and increments a count on repeats. Define deletion as decrement-or-remove. Repeated nodes need a consistent side policy and careful validation/balancing; document whether rank counts unique keys or all occurrences.'),
    C('A service requires predictable fast lookups while keys arrive in sorted order. Evaluate an ordinary BST, a balanced BST, and a hash map when range queries are also required.', 'An ordinary BST can become linear. A balanced BST provides O(log n) search/updates and ordered range traversal. A hash map offers expected constant lookup but no inherent efficient ordered ranges. Choose according to ordering needs and guarantees, not average lookup alone.'),
  ]),
  'tree-dfs':B([
    M('Tree DFS primarily explores:', ['Every level before the next','One branch deeply before backtracking','Only leaves','Only the root and its children'],1,'Depth-first search follows a branch, then returns to explore other branches.'),
    S('What structures can hold the pending work for tree DFS?', 'The method call stack in a recursive implementation, or an explicit stack in an iterative one.'),
    S('Preorder DFS visits root A with left child B and right child C, visiting left first. What is the order?', 'A, B, C. Preorder processes the current node before its children.'),
    C('Find whether a binary tree contains value 9 using DFS. What should a null subtree return?', 'A null subtree returns false. At a node, check its value, then search children until a match. Worst-case O(n) time and O(h) recursive space.'),
    M('For recursive DFS on a tree of height h and n nodes, maximum active call-stack space is usually:', ['O(1)','O(h)','O(n²)','O(log h)'],1,'Only the current ancestor chain is active at once. On a chain, h itself can be O(n).'),
    S('An iterative preorder traversal must visit left before right. In which order should it push children onto a LIFO stack?', 'Push the right child first, then the left child. The left child will be popped and visited first.'),
    C('For a root-to-leaf sum test, why is reaching a target sum at an internal node insufficient?', 'The contract requires a complete root-to-leaf path. Accept only when the sum matches at a leaf; otherwise continue down the remaining branches.'),
    C('Count root-to-leaf paths summing to 7 in a tree: root 3; left leaf 4; right child 2 with leaves 2 and 5.', 'There are 2: 3→4 and 3→2→2. Pass the accumulated or remaining sum by value, checking only leaves. O(n) traversal time and O(h) stack.'),
    D('Design DFS to return every root-to-leaf path without sharing mutable result lists. Give complexity in terms of total output length L.', 'Maintain one working path, append on entry, copy at a leaf, and remove on exit. Time is O(n+L); results need O(L), with O(h) traversal/path space. Copying each result is necessary because the working path changes.'),
    C('DFS is used on a “tree” assembled from node references, but a child accidentally points to an ancestor. What behavior changes, and how would you diagnose the structure?', 'Naive recursion can loop until stack overflow. Track identities on the active path to detect cycles; track previously seen nodes to detect shared children if a strict tree is required. Distinguish invalid input from a traversal bug and report the offending link.'),
  ]),
  'tree-bfs':B([
    M('Which data structure naturally supports level-order traversal of a tree?', ['A FIFO queue','A min-only scalar','A sorted array of values','A LIFO stack alone'],0,'The queue processes nodes in discovery order, so children follow all pending nodes in the current level.'),
    S('Root A has children B and C; B has child D. What is the left-to-right level-order sequence?', 'A, B, C, D, assuming children are enqueued left to right.'),
    S('What should level-order traversal return for an empty tree?', 'An empty result, without enqueuing or dereferencing a null root.'),
    C('How can a queue-based BFS collect separate lists for each level without storing a depth alongside every node?', 'At the start of a level, capture the current queue size. Remove exactly that many nodes while adding their children; newly added nodes belong to the next level.'),
    M('BFS queue space is most closely related to:', ['The number of distinct values','The tree’s maximum width','Only the root degree','Always constant space'],1,'The queue holds nodes around the current frontier. A wide tree can require O(n) space.'),
    S('Find the minimum root-to-leaf depth in nodes for root 1, left leaf 2, and right chain 3→4→5. Explain why BFS can stop early.', 'Depth 2. BFS reaches the left leaf at the earliest possible level, so no later leaf can be shallower.'),
    C('Compute the average at each level of root 4, children 2 and 8, with leaves 1 and 3 beneath 2.', 'Averages are 4, 5, and 2. Sum values for each captured level size and divide using floating-point arithmetic; use a wide sum type if values/counts can be large.'),
    C('Compare BFS and recursive DFS memory for a million-node chain versus a wide balanced tree. Which is safer under each shape?', 'BFS has a tiny frontier on a chain while recursive DFS has deep stack risk. In a wide balanced tree BFS may store a large level, while DFS needs O(log n) active depth. Consider explicit DFS stacks and actual shape rather than choosing universally.'),
    D('Design zigzag level-order output while keeping the queue’s child discovery order conventional.', 'Use normal left-to-right BFS and capture each level size. Place level values at index i or size−1−i according to alternating direction. Keep enqueue order consistent; time O(n), auxiliary frontier/level space O(w), excluding results.'),
    C('A level traversal uses `for (int i=0; i<queue.size(); i++)` while removing nodes and adding children. Explain the bug and a test that exposes it.', 'The loop bound changes during processing, mixing levels or skipping nodes. Capture size before the loop. A root with two children, one of which has grandchildren, exposes changing queue lengths and incorrectly grouped levels.'),
  ]),
  traversals:B([
    M('Which traversal visits left subtree, node, then right subtree?', ['Preorder','Inorder','Postorder','Level order'],1,'Inorder places the current node between its left and right subtrees.'),
    S('State the visit order rules for preorder and postorder in a binary tree.', 'Preorder: node, left, right. Postorder: left, right, node.'),
    S('For root 2 with left leaf 1 and right leaf 3, give the postorder traversal.', '1, 3, 2. Both children are processed before the root.'),
    C('A tree has root A, left child B with right child D, and right leaf C. Give preorder, inorder, and postorder.', 'Preorder A,B,D,C; inorder B,D,A,C; postorder D,B,C,A. The missing left child of B contributes no value.'),
    M('Which traversal is natural for computing a subtree result that depends on completed results from both children?', ['Postorder','Preorder only','Random order','Root-only visitation'],0,'Postorder computes child results first, then combines them at the parent.'),
    S('Why can inorder traversal sort BST keys but not the values of an arbitrary binary tree?', 'Sorted output depends on the BST ordering invariant. A general binary tree can place values on either side without any ordering relation.'),
    C('Reconstruct the unique distinct-key binary tree with preorder [3,1,2,4] and inorder [1,2,3,4].', 'Root is 3. Its left subtree contains 1,2 and has root 1 with right child 2; its right child is 4. Split inorder at each preorder root.'),
    D('Explain why preorder alone cannot reconstruct every binary tree. Supply a two-node counterexample.', 'Preorder [A,B] can describe A with left child B or A with right child B. Null markers or additional suitable traversal/shape information are required.'),
    C('Design an O(n)-time reconstruction from preorder and inorder with distinct keys. What input validation and space costs should be considered?', 'Map each inorder key to its index, advance a preorder cursor, and recursively build valid index ranges. Check lengths, duplicates, key membership, and that each root lies in its expected range. Use O(n) map/output space and O(h) recursion space.'),
    C('You need postorder iteratively without modifying nodes. Compare a two-stack solution with a one-stack solution that tracks the previously visited node.', 'Two stacks reverse a root/right/left-style accumulation and use O(n) storage. One stack plus lastVisited can defer a node until its right subtree completes, using O(h) pending ancestors. Explain node identity comparisons and test single-child chains and an empty tree.'),
  ]),
};
