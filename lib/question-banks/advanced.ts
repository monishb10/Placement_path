import {B,M,S,C,D,type Question} from './types';

export const advancedQuestions: Record<string,Question[]> = {
  heap:B([
    M('What does a min-heap guarantee about its root?', ['It is the largest element','It is the smallest element','It is the most recently added element','It is the median'],1,'Each parent is no greater than its children, making the root a global minimum. The full array is not sorted.'),
    S('What is the default priority ordering of a Java PriorityQueue<Integer> without a custom comparator?', 'Natural ascending order: peek/poll exposes the smallest integer first.'),
    S('Insert 8, 3, and 5 into a min-priority queue. What do two successive polls return?', '3, then 5. Poll removes the current minimum each time.'),
    C('Explain the time costs of heap peek, insertion, and removal of the minimum for n stored items.', 'Peek is O(1). Sifting up or down costs O(log n), as does root removal. In an array-backed heap, insertion may occasionally also copy O(n) entries during growth; across insertions its usual amortized cost is O(log n).'),
    M('To keep the largest k values seen so far using a heap of size k, which root is most useful?', ['The smallest retained value','The largest discarded value','The latest input value','A random retained value'],0,'A min-heap exposes the weakest of the retained top k values so it can be replaced by a larger arrival.'),
    S('Find the third largest value in [7,2,9,4,8], counting elements rather than distinct values, with a size-3 heap.', '7. Retain the three largest values {7,8,9}; the minimum of that heap is the third largest. Time O(n log k), space O(k).'),
    C('Why should an application not iterate over a PriorityQueue and assume it sees values in sorted order?', 'The heap invariant orders parents relative to children, not all positions. Repeated polling yields priority order, but modifies the queue; copy first if preservation is required.'),
    C('Merge k already sorted lists containing N elements total with a priority queue. Describe what each heap entry stores and the cost.', 'Store each current head with its list identity or next reference. Poll the minimum, emit it, and insert its successor. Time O(N log k), heap space O(k), plus output storage. Define a tie-breaker if stable cross-list ordering is required.'),
    D('Design a running-median structure for integer inputs and explain the invariants and overflow issue when the count is even.', 'Maintain a max-heap of the lower half and min-heap of the upper half, with size difference at most one and all lower values≤upper values. Rebalance on insertion. Median is a root or their average; widen before adding. Insert O(log n), median O(1), storage O(n).'),
    C('A task already inside a Java PriorityQueue has its priority field changed. Why may peek now be wrong, and how would you support updates?', 'Mutating a key does not automatically restore heap order. Remove and reinsert, noting arbitrary removal may be O(n), or use an indexed heap. Lazy invalidation with versioned entries is another option if stale entries are safely skipped and storage growth is controlled.'),
  ]),
  greedy:B([
    M('A greedy algorithm typically makes:', ['A locally preferred choice at each step','Every possible choice simultaneously','Only recursive calls','A random choice with no criterion'],0,'It commits to a locally best-looking choice. Correctness requires proving that these choices can lead to a global optimum.'),
    S('Why is “choose the largest available coin” not automatically correct for every coin system?', 'Local choices may block a better combination. Correctness depends on the denominations, not on the greedy idea alone.'),
    S('For denominations 1, 5, 10 and amount 18, list the coins chosen by largest-first greedy.', '10,5,1,1,1: five coins. This traces the rule; it does not prove that the rule works for arbitrary denominations.'),
    C('With denominations 1,3,4 and amount 6, compare largest-first greedy with an optimal solution.', 'Greedy chooses 4+1+1, three coins. Optimal is 3+3, two coins. This is a concrete counterexample to a universal coin-greedy claim.'),
    M('To maximize the number of nonoverlapping intervals on one resource, assuming touching endpoints are allowed and intervals have positive duration, which standard greedy rule works?', ['Choose the earliest finishing compatible interval','Choose the longest interval first','Choose the latest starting interval first in a forward scan','Choose an arbitrary interval'],0,'Earliest finish leaves maximum room for later intervals; an exchange argument can justify the choice.'),
    S('Select the maximum number of compatible intervals from [1,3), [2,5), [3,4), [4,6), where touching is allowed.', 'Choose [1,3), [3,4), [4,6): three intervals. Sorting by finish time and accepting start≥lastEnd produces this schedule.'),
    C('A fractional knapsack has capacity 10 and items (weight,value)=(5,30),(10,40). Fractions are allowed. Give the greedy result.', 'Take all of the ratio-6 first item, then half of the ratio-4 second item. Value=30+20=50. The ability to split items is essential.'),
    D('Outline an exchange proof for selecting the earliest-finishing interval in unweighted interval scheduling.', 'Take an optimal schedule’s first interval. Replacing it with the globally earliest-finishing interval does not delay its end, so all later intervals remain compatible and the count is unchanged. Apply the same argument to the remaining compatible intervals.'),
    C('Intervals carry rewards: [0,2) worth 2, [2,4) worth 2, and [0,4) worth 10. Does earliest-finish selection maximize reward? Propose a suitable approach.', 'No: it obtains 4 rather than 10. Weighted interval scheduling can sort by finish time, find the previous compatible interval, and use DP to choose skip versus take. Optimize compatibility lookup with binary search.'),
    C('Given ropes of lengths 2,3,4, joining two costs their combined length, and the new rope can be joined again. Minimize total cost and justify the strategy at a high level.', 'Join 2+3=5, then 5+4=9, total 14. Repeatedly merge the two smallest with a min-heap. Small lengths should incur repeated costs deeper in the merge tree; an exchange argument supports that arrangement. O(n log n) time.'),
  ]),
  graphs:B([
    M('What distinguishes a directed graph from an undirected graph?', ['Its vertices must be numbers','An edge has an orientation from one vertex to another','It cannot contain cycles','Every vertex has one neighbor'],1,'A directed edge u→v does not imply a reverse edge v→u.'),
    S('Define vertex, edge, and path using a road-network example.', 'Vertices can be junctions, edges road connections, and a path a sequence of adjacent junctions. One-way roads correspond to directed edges.'),
    S('An undirected graph has edges A–B and B–C. What are the degrees of A, B, and C?', '1,2,1. Each edge contributes one incidence to each endpoint, so the degree sum is 4.'),
    C('Compare adjacency-list and adjacency-matrix storage for a simple graph with V vertices and E edges.', 'Lists use O(V+E) storage and efficiently enumerate neighbors. A matrix uses O(V²) storage but checks a specific edge in O(1). A list edge check may scan a vertex’s neighbors.'),
    M('A graph in which some vertices cannot reach others through any undirected path is:', ['Complete','Disconnected','Necessarily cyclic','A binary tree'],1,'It has multiple connected components. Being disconnected does not determine whether any component contains a cycle.'),
    S('When storing an undirected edge u–v in adjacency lists, which entries are usually added?', 'Add v to u’s list and u to v’s list. Special handling may be needed for self-loops or duplicate edges according to the graph model.'),
    C('Model course prerequisites as a graph. What does a directed cycle mean, and what output is needed if no cycle exists?', 'Use prerequisite→dependent edges. A directed cycle makes all prerequisites in that cycle impossible to satisfy in sequence. A topological ordering gives one valid course order if the graph is acyclic.'),
    C('For V=100,000 and E=200,000, justify a graph representation for neighbor traversals and estimate the scale difference from a matrix.', 'An adjacency list stores O(V+E) entries, roughly 400,000 neighbor entries for undirected edges. A matrix has 10 billion cells before representation overhead. Lists suit this sparse graph; actual memory also depends on language/object layout.'),
    D('A social network stores friendship, following, and distance between cities. Evaluate whether one undirected unweighted graph accurately models all three.', 'Friendship may be undirected, following is directed, and road distance requires weights and possibly direction. Separate edge semantics or typed graphs; define what each path query means so algorithms do not mix incompatible relationships.'),
    C('Design input validation and traversal rules for a graph that may contain isolated vertices, repeated edges, and self-loops. How do the problem requirements affect your choices?', 'Represent all declared vertices, including isolated ones. Reject or retain duplicates/self-loops according to whether the graph is simple or a multigraph. Validate endpoint ranges; track visited states so reachability terminates. Degree, cycle, and edge-count results depend on these choices.'),
  ]),
  'graph-traversal':B([
    M('Which traversal guarantees shortest paths by edge count from one source in an unweighted graph?', ['Ordinary DFS','BFS','Any arbitrary walk','Sorting vertex labels'],1,'BFS explores distance layers. The guarantee is about equal-cost edges, not arbitrary weighted edges.'),
    S('Why do graph traversals usually need a visited structure even when a tree traversal does not?', 'Graphs can have cycles and several paths to the same vertex. Visited tracking avoids repeated work and infinite revisits.'),
    S('Start BFS at A with edges A–B, A–C, B–D, exploring B before C. Give the visit order.', 'A,B,C,D. Mark each vertex when discovering/enqueuing it to avoid duplicate queue entries.'),
    C('A graph contains components {0,1,2} and {3,4}. Why does one DFS from 0 not visit every vertex, and how do you count components?', 'DFS reaches only its source component. Loop through all vertices and start a new traversal at each unvisited vertex; the number of starts is 2.'),
    M('With adjacency lists and each vertex visited once, BFS or DFS over a graph costs:', ['O(V+E) time','O(VE) always','O(log V) time','O(1) time'],0,'Each vertex and adjacency entry is processed a bounded number of times. Undirected edges appear twice, only a constant factor.'),
    S('How can BFS reconstruct an actual shortest path after finding a target?', 'Store a parent when first discovering each vertex. Follow parents from target back to source and reverse the sequence; report unreachable when the target was never discovered.'),
    C('In an undirected DFS, an already visited neighbor is not always evidence of a new cycle. Which edge must be distinguished?', 'The edge back to the current node’s parent. In a simple undirected graph, skip that parent edge; another visited neighbor indicates a cycle. Multigraphs require tracking edge identity to handle parallel edges correctly.'),
    C('Edges are S→A weight 10, S→B weight 1, B→A weight 1. Explain why ordinary BFS does not compute minimum total weight.', 'BFS prefers the one-edge path S→A, costing 10, over the two-edge path costing 2. For nonnegative weights use an algorithm such as Dijkstra that prioritizes cumulative distance.'),
    D('Design a BFS test for whether every component of an undirected graph is bipartite. What evidence proves failure?', 'Start from every uncolored vertex, assign alternating colors across edges, and reject an edge joining equal colors. Such a conflict corresponds to an odd cycle; self-loops immediately fail. Time O(V+E), storage O(V).'),
    C('A grid has walkable cells and walls; multiple sources spread simultaneously one step per minute. How do you compute each reachable cell’s earliest arrival time?', 'Enqueue all sources initially at distance 0, then perform multi-source BFS over allowed neighbors. First discovery gives minimum distance from any source. Mark walls and visited cells correctly; unreachable cells keep a sentinel. O(rows×cols) time and space.'),
  ]),
  'union-find':B([
    M('Disjoint Set Union primarily maintains:', ['Sorted order of all keys','Groups of connected elements under merges','Shortest weighted paths','A stack of function calls'],1,'DSU tracks component membership with find and combines components with union.'),
    S('What does find(x) return in a standard parent-pointer DSU?', 'A representative/root of x’s current set. Different elements in the same set have the same representative.'),
    S('Start with sets {0},{1},{2},{3}. After union(0,1) and union(2,3), how many components remain?', '2 components: {0,1} and {2,3}. Each successful merge of different sets reduces the count by one.'),
    C('Explain path compression during find and why it preserves connectivity.', 'After finding the root, redirect nodes along the searched path to that root. They stay in the same component; only the internal parent tree becomes shallower.'),
    M('Union by size attaches:', ['The larger component below the smaller one','The smaller component’s root below the larger one','Every node directly to the newest node','Only adjacent array indices'],1,'Attaching the smaller tree under the larger limits depth growth. Component representatives must be found first.'),
    S('If find(a)==find(b), what should union(a,b) do to the component count?', 'Leave it unchanged. The elements already belong to the same component.'),
    C('Process undirected edges (0,1),(1,2),(2,0) using DSU. Which edge first reveals a cycle?', '(2,0). The first two edges merge all three vertices; the third joins vertices already in the same component. This direct rule is for undirected cycle detection.'),
    C('Explain how Kruskal’s minimum spanning tree algorithm uses DSU and where its main time cost occurs.', 'Sort edges by increasing weight; add an edge only when its endpoints are in different sets, then union them. Sorting costs O(E log E); DSU with size/rank and compression has near-constant amortized operations. A disconnected input yields a forest.'),
    D('Can a standard DSU answer arbitrary edge-deletion connectivity queries efficiently? Explain the limitation and a possible offline strategy.', 'Ordinary union does not store enough information to split a component. If all deletions are known in advance, sometimes process operations backward as additions. More general offline problems may use rollback DSU; online dynamic connectivity needs additional machinery.'),
    C('A grid starts entirely water and cells become land one at a time. Design DSU-based island counting, including duplicate additions.', 'On first activation create one component, then union with active orthogonal neighbors, decrementing the count for each successful merge. Ignore repeated activation of the same cell. Keep active flags separate from DSU parents; use cell-to-index mapping and validate boundaries.'),
  ]),
  dp:B([
    M('Dynamic programming is especially useful when a problem has:', ['Only distinct unrelated calculations','Overlapping subproblems and a composable optimal/counting structure','No base cases','Only one possible input'],1,'DP defines states and reuses subproblem results instead of recomputing them. Optimization problems also need appropriate optimal substructure.'),
    S('What is the difference between memoization and tabulation?', 'Memoization computes states on demand and caches them, usually through recursion. Tabulation fills a table iteratively in a valid dependency order.'),
    S('Why must a DP solution define what one state entry means before writing transitions?', 'A precise state meaning determines dependencies, base cases, and the final answer. An unclear state often mixes incompatible subproblems or permits illegal choices.'),
    C('A recurrence needs only dp[i−1] and dp[i−2]. Explain when storage can be reduced from O(n) to O(1).', 'If only the final value is required, keep the previous two values and update them carefully. Retaining all values or reconstructing decisions may require extra information.'),
    M('A DP has n states, each trying at most k transitions in constant time. Ignoring initialization, its time is:', ['O(n+k) always','O(nk)','O(log n)','O(2ⁿ) necessarily'],1,'The usual analysis is number of evaluated states multiplied by work per state. Count hidden loops or expensive operations too.'),
    S('A memo table uses 0 to mean “not computed,” but 0 is a valid result. What problem can occur and how can it be fixed?', 'Zero-valued states may be recomputed repeatedly. Use a separate visited array or an impossible sentinel that cannot conflict with valid results.'),
    C('For minimum coins to make amount 6 using denominations 1,3,4 with unlimited copies, define a state and transition.', 'dp[a] is the minimum coins for amount a. Set dp[0]=0 and dp[a]=1+min(dp[a−c]) over usable denominations with reachable prior states. dp[6]=2. Guard unreachable sentinels against overflow.'),
    D('An interview solution says “DP is O(n)” but stores states by index and remaining budget. How would you evaluate its actual complexity?', 'Count distinct (index,budget) states and transitions. With n items and budget B, there may be O(nB) states, not n. Discuss reachable-state sparsity, memory, recursion depth, and whether B is a numeric value rather than input bit length.'),
    C('A planner maximizes total reward but must choose exactly three tasks. A DP state stores only the task index. Explain the missing information and a repair.', 'Future choices depend on how many tasks have been selected. Include selected count (or remaining count) in the state; distinguish impossible states from zero reward. Handle negative rewards and enforce exactly three in the base/final condition.'),
    C('Two correct DP implementations return the same optimum, but one must also return the chosen actions. Design reconstruction and explain a memory trade-off.', 'Store a chosen transition or recover it from saved state values, then walk backward/forward through dependencies. Define tie-breaking for multiple optima. Rolling arrays may discard reconstruction information, requiring parent storage or a deliberate recomputation strategy.'),
  ]),
  fibonacci:B([
    M('With F(0)=0 and F(1)=1, which recurrence defines Fibonacci numbers for n≥2?', ['F(n)=2n','F(n)=F(n−1)+F(n−2)','F(n)=n×F(n−1)','F(n)=F(n−1)−1'],1,'Each term is the sum of its two predecessors.'),
    S('List F(0) through F(6) under the standard 0,1 base cases.', '0,1,1,2,3,5,8. Starting from 0 and 1, add the preceding two values to produce each next term.'),
    S('What repeated subproblem appears when recursively evaluating F(5) without caching?', 'F(3) is evaluated directly inside F(5)’s F(3) branch and again inside its F(4) branch; smaller states repeat even more.'),
    C('Compare plain recursive Fibonacci with memoized Fibonacci in terms of repeated work and asymptotic time.', 'Plain recursion repeats a branching tree of calls and takes exponential time, commonly bounded by O(2ⁿ). Memoization computes n+1 states once for O(n) time and O(n) storage/stack.'),
    M('An iterative Fibonacci method stores only the two previous terms. Excluding numeric bit width, its auxiliary space is:', ['O(n²)','O(n)','O(log n)','O(1)'],3,'It uses a fixed number of numeric variables rather than an n-entry table.'),
    S('A rolling implementation sets a=b and then b=a+b. Why is this update incorrect without preserving the old values?', 'The second statement uses the newly overwritten a, producing 2×old b. Compute next=old a+old b before shifting a and b.'),
    C('The contract accepts n=0. A method initializes result=1 and loops from 2 to n. Identify the error and the needed boundary handling.', 'It returns 1 for F(0), but F(0)=0. Return n for n≤1 after rejecting negative inputs, or choose initial variables and a loop consistent with all base cases.'),
    C('An application requests exact Fibonacci values for n=1000. Evaluate using Java int, long, and BigInteger, including complexity claims.', 'int and long overflow. BigInteger supports exact values, whose bit length grows with n. A rolling algorithm uses O(n) additions, but bit-operation time and memory are not constant per addition; avoid claiming fixed-byte O(1) storage.'),
    D('Suppose you need F(n) modulo m for a very large nonnegative n. Outline how matrix exponentiation improves the number of recurrence steps.', 'The matrix [[1,1],[1,0]] transforms consecutive terms. Binary exponentiation computes its nth power with O(log n) fixed-size matrix multiplications. Reduce modulo m and ensure intermediate multiplication cannot overflow the chosen numeric representation.'),
    C('A service receives many Fibonacci queries with n≤100,000, all modulo 1,000,000,007. Compare precomputation with recalculating each query.', 'Precompute one table in O(N) recurrence steps and O(N) storage, then answer each query in O(1). Repeated rolling computations save the table but cost O(sum of queried n). Use safe addition before reducing and specify base cases.'),
  ]),
  climbing:B([
    M('You climb n stairs with moves of 1 or 2. If order matters, the number of ways satisfies:', ['ways(n)=n²','ways(n)=ways(n−1)+ways(n−2)','ways(n)=2×ways(n−2) only','ways(n)=ways(n−1)−1'],1,'Every valid route ends in a 1-step or a 2-step move, giving disjoint predecessor cases.'),
    S('List all ordered ways to climb 3 stairs using steps of 1 or 2.', '1+1+1, 1+2, and 2+1: three ways.'),
    S('Why is ways(0)=1 a useful counting base case?', 'There is one empty sequence of moves. It lets a move that exactly reaches the target contribute one valid route.'),
    C('Compute the number of ways to climb 5 stairs with moves of 1 or 2 and show the DP values from 0.', 'Values are 1,1,2,3,5,8, so there are 8 ways. Each value sums the previous two.'),
    M('For the ordinary 1-or-2-step problem, which implementation reaches O(n) time and O(1) auxiliary numeric slots?', ['Enumerate every route','Store only two previous counts','Sort all possible moves repeatedly','Use uncached recursion'],1,'The count at i needs only counts at i−1 and i−2.'),
    S('How does the recurrence change if allowed moves are 1,2,3?', 'ways(i)=ways(i−1)+ways(i−2)+ways(i−3), with ways(0)=1 and negative indices treated as zero.'),
    C('There are 4 stairs, moves are 1 or 2, and landing on stair 2 is forbidden. Count valid routes.', 'Only 1→3→4 is valid, so 1 route. Set ways(2)=0 and compute other states normally: ways(0..4)=1,1,0,1,1.'),
    C('A climber may move 1 or 2 stairs but cannot take two 2-step moves consecutively. Design a state that counts routes to stair n.', 'Track position and whether the previous move was 2. A 1-step move is always allowed and clears the flag; a 2-step move is allowed only when the flag is false. Start at position 0 with no previous 2. O(n) states/time.'),
    D('A stair-counting answer is too large for long. Compare exact arithmetic with counting modulo a specified positive integer.', 'BigInteger preserves the exact count with growing arithmetic cost. Modular counting stores only residues and suits a contract asking for a modulus, but cannot recover the exact count. Apply the modulus at each addition and protect intermediate sums from overflow.'),
    C('Stairs have nonnegative costs [10,15,20]. You may start at index 0 or 1, pay when stepping on an index, and move 1 or 2 indices to reach position 3. Find minimum cost and distinguish this from counting routes.', 'Minimum cost is 15 by starting at index 1 then moving to position 3. Define a minimum-cost state with min over predecessors; counting uses sums instead. State whether the top has a cost and how starts are initialized.'),
  ]),
  knapsack:B([
    M('In 0/1 knapsack, each item may be chosen:', ['Any fractional amount','At most once','An unlimited number of times','Only if its value equals its weight'],1,'An item is either selected once or excluded. This differs from fractional and unbounded knapsack.'),
    S('What do capacity, item weight, and item value represent in a simple delivery selection problem?', 'Capacity is the maximum total weight allowed. Each package consumes its weight and contributes its value if selected; maximize value without exceeding capacity.'),
    S('With capacity 3 and items (weight,value)=(2,4),(3,5), what is the best achievable value?', '5, by taking the weight-3 item. Both items together exceed capacity.'),
    C('For items (2,3),(3,4),(4,5) and capacity 5, find the optimal 0/1 selection.', 'Select the first two items, total weight 5 and value 7. The weight-4 item alone gives only 5.'),
    M('A standard 0/1 knapsack DP with n items and integer capacity W has:', ['O(nW) time','O(n+W) time always','O(log n) time','O(2W) time independent of n'],0,'Each item/capacity state considers taking or skipping that item in constant time.'),
    S('Define dp[i][c] for 0/1 knapsack and give its take/skip transition.', 'dp[i][c] is the maximum value from the first i items within capacity c. Skip uses dp[i−1][c]; if weight fits, take uses value+dp[i−1][c−weight]. Choose the maximum.'),
    C('Why must a one-dimensional 0/1 knapsack table update capacities downward for each item?', 'Descending capacity reads values from before the current item was used. Ascending capacity can reuse its newly updated state, accidentally allowing multiple copies.'),
    C('With one item of weight 2 and value 3, capacity 4, demonstrate the ascending-update bug.', 'An upward loop sets dp[2]=3, then dp[4]=dp[2]+3=6, using the one item twice. The correct 0/1 result is 3; iterate from capacity down to weight.'),
    D('Capacity W may be one billion, but there are only 30 items. Evaluate O(nW) DP and suggest alternatives with their trade-offs.', 'Capacity DP is impractical in time and memory. Meet-in-the-middle enumerates roughly 2^(n/2) subsets per half, then combines feasible nondominated choices. Value-based DP may help if total values are small. Choose based on actual constraints; O(nW) is pseudo-polynomial.'),
    C('A knapsack must be filled to exactly W, and values may be negative. Explain why initializing every DP entry to 0 gives incorrect answers.', 'Zero would falsely mark unreachable capacities as feasible and may ignore required negative-value choices. Initialize only capacity 0 as reachable and other capacities as impossible; transition only from reachable states and report impossibility if W remains unreachable.'),
  ]),
  subset:B([
    M('The decision version of subset sum asks whether:', ['All elements must be sorted','Some subset sums exactly to a target','Only a contiguous segment sums to a target','Every element must be selected'],1,'A subset can choose elements from anywhere, usually using each given position at most once.'),
    S('Does [3,5,7] have a subset summing to 8? Identify one if it exists.', 'Yes: 3+5=8. The elements need not form a separate sorted or contiguous structure.'),
    S('Why is target 0 reachable when the empty subset is permitted?', 'Selecting no elements gives sum 0. This establishes the initial true state for a boolean DP.'),
    C('For nonnegative [2,3,7], list reachable sums up to 5 after processing only the first two values.', '0,2,3,5. Start with 0 and, for each value, add it to previously reachable sums without reusing that item.'),
    M('A boolean DP over n nonnegative values and target T commonly uses which optimized bounds?', ['O(nT) time and O(T) space','O(T) time and O(1) space always','O(log n) time and O(n) space','O(n²T²) time necessarily'],0,'Track reachable sums through T and update once per item. T must be small enough for this table.'),
    S('How do you reduce equal-partition of nonnegative integers to subset sum?', 'Compute total S. If S is odd, equal partition is impossible; otherwise ask whether a subset sums to S/2. The complement then has the same sum.'),
    C('For a one-dimensional boolean table, explain why sums must be updated downward when each input position can be used once.', 'Reading lower sums that have not yet been changed for this item prevents reuse. Upward updates can repeatedly use the same value, solving an unbounded variant instead.'),
    C('Subset sum includes negative values, such as [4,-3,2] with target 1. Why is a table only indexed 0 through target invalid, and what can replace it?', 'Intermediate reachable sums may be negative or greater than the target; 4+(−3)=1 must remain possible. Use an offset over the full attainable sum range or a set of reachable sums, using a snapshot for each item.'),
    D('Convert a decision-only subset-sum DP into one that returns an actual chosen subset. Discuss duplicate values and item identity.', 'Store predecessor sum and item index for newly reached states, or reconstruct from a 2D table. Track indices, not only values, to distinguish duplicates and prevent reusing a position. Reconstructed sums and selection counts should be validated.'),
    C('Count subsets by distinct index choices for [0,0,1] with target 1. Explain a trap in counting zero-valued items.', 'There are 4: choose the 1 and independently include/exclude each zero. Each zero doubles the number of ways to each already reachable sum. A boolean DP loses this multiplicity; counting transitions must preserve separate index choices.'),
  ]),
  lis:B([
    M('A subsequence differs from a contiguous subarray because it:', ['May skip elements while preserving order','May rearrange elements arbitrarily','Must contain every element','Must start at index 0'],0,'Subsequence indices increase, but adjacent selected elements need not be adjacent in the input.'),
    S('What is the length of a strictly increasing subsequence of maximum length in [3,1,2]?', '2, from [1,2]. The original order prevents using 1,2,3.'),
    S('For [2,2,2], what is the strictly increasing LIS length, and how would a nondecreasing version differ?', 'Strict LIS length is 1. A nondecreasing subsequence permits equality, so its length is 3.'),
    C('Define the O(n²) LIS state for a subsequence ending exactly at index i and its transition.', 'dp[i] starts at 1. For each j<i with a[j]<a[i], consider dp[j]+1. The result is max dp[i], or 0 for empty input.'),
    M('In the O(n log n) strict-LIS tails method, replace which position with a new value x?', ['The first tail ≥ x','The first tail > x only','The last tail < x always','Every tail equal to x'],0,'Lower bound preserves the smallest possible tail per length and prevents equal values from extending a strictly increasing sequence.'),
    S('Trace strict-LIS tails for [3,4,1,2]. What final length results?', 'Tails evolve [3], [3,4], [1,4], [1,2]. Length 2. Replacements improve future extension opportunities without claiming tails itself is always a valid reconstructed subsequence.'),
    C('Find one LIS of [10,9,2,5,3,7,101,18] and state the length.', 'One is [2,3,7,18], length 4; [2,5,7,101] is another. Multiple optimal subsequences can exist.'),
    D('Justify why keeping the smallest tail for each subsequence length does not lose a future opportunity to extend that length.', 'Among equal-length increasing subsequences, the one ending smaller can accept every future value that a larger tail could accept, and possibly more. Maintain increasing tails and explain binary-search replacement; this establishes a dominance argument.'),
    C('An interviewer asks for the LIS sequence, not just its length, in O(n log n) time. What additional bookkeeping is needed?', 'Store the input index representing each tail length and a predecessor index for each element. When replacing/extending, link to the prior length’s index. Follow predecessors from the final tail and reverse. This requires O(n) extra storage.'),
    C('Choose the largest chain of envelopes, where both width and height must strictly increase and rotation is forbidden. Explain how to reduce it to LIS without incorrectly chaining equal widths.', 'Sort width ascending and height descending within equal widths, then find a strict LIS of heights. Descending ties prevent taking two equal-width envelopes. Justify both directions of the reduction and test duplicates/equal dimensions.'),
  ]),
  'grid-dp':B([
    M('A robot moves only right or down through a rectangular grid. Which previous cells can reach an interior cell directly?', ['Left and above','Right and below','All eight neighbors','Only the diagonal'],0,'A right move comes from the left; a down move comes from above.'),
    S('How many paths go from the top-left to the bottom-right of a 2×2 grid using only right/down moves and no obstacles?', '2: right then down, or down then right.'),
    S('What should the path count be if the starting cell is blocked?', '0. No valid route can begin, regardless of the remaining cells.'),
    C('Give the counting recurrence for right/down paths with blocked cells and explain the start initialization.', 'Blocked cells have 0 ways. The unblocked start has 1; other cells sum ways from above and left, treating out-of-grid predecessors as 0.'),
    M('For an R×C grid with constant work per cell, tabulation takes:', ['O(R+C) time always','O(RC) time','O(2^(RC)) time necessarily','O(log R+log C) time'],1,'Every cell is processed once. Space can often be reduced from O(RC) to O(C) when only the previous row is needed.'),
    S('A 3×3 grid blocks only the center cell. How many right/down paths connect opposite corners?', '2. The valid routes go along the top then right edge, or the left then bottom edge.'),
    C('Find the minimum path sum, including start/end cells, in [[1,3,1],[1,5,1],[4,2,1]], moving only right/down.', '7 via 1→3→1→1→1. Use cost[r][c]+min(best above,best left), with careful border initialization.'),
    C('In one-row obstacle-path DP, why must dp[c] be set to 0 at a blocked cell instead of merely skipping its update?', 'Before the update dp[c] contains ways from the cell above. Leaving it unchanged allows paths to pass through the obstacle and contaminate later cells. Reset to 0 to remove all routes through the blocked cell.'),
    D('A robot may now move in all four directions, and you want a shortest path in an unweighted grid. Evaluate whether the ordinary row-by-row grid-DP recurrence still applies.', 'It no longer has an acyclic above/left dependency order; movement introduces cycles. BFS with visited/distance tracking computes shortest paths. If counting paths is requested, define simple paths or length limits before assuming a finite counting recurrence.'),
    C('A right/down path may remove at most one obstacle. Design a DP state, including what happens when the start is blocked.', 'Use ways[r][c][used], where used is 0 or 1. An open cell preserves used; a blocked cell transitions only from used=0 to 1. At a blocked start initialize the used=1 state. Sum both end states; O(RC) time with a constant state factor.'),
  ]),
};
